# 2026-07-28

- theme-pack-component-skins：開新 todo——主題資產規格擴「元件裝飾」（華麗送出鈕貼皮＋動效），引擎進開源碼、資產進贊助包，定位主題包 v2 回購誘因；排 release-4-theme-pack 之後，格式定案時先預留 schema。
- stable-id-storage：四題拍板（發言者存代碼＋名字快照／桌角色都改／ULID crate／不遷移＋state.json 欄位必填擋舊資料）；執行安排：主線寫計畫→發包 Opus subagent，圖庫實測與改名提示複驗併到本案後驗。
- cli-connected-badge：補上 Mac 缺的驗證回傳通道（腳本 touch `.verified-<id>` 印記＋`cli_verified` 指令＋前端輪詢讀印記才停），grok 探針從 `grok -p "ok"`（實測 26.0 秒，逼近 30 秒上限）換成 `grok models`（0.8 秒）——Mac 用 grep 判登入字串、Windows 新增 `InstallSpec.probe_expect` 在 Rust 端比對，grok 的 Windows `pre_probe` 一併翻 true；設定頁「已連結 ✓」對齊修正。cargo 99 綠＋npm build 綠＋tsc 無錯，Mac DMG 實機實測四家 CLI 全通過。commit c9993af
- character-card-avatar-issues：角色卡改名提示複驗——欄位下方灰字確實會出現但實測兩輪都沒被注意到（時機不對，不是字太小），改成按儲存時跳原生確認框並帶入新舊名，實機實測通過。commit c9993af
- 結案四項：cli-connected-badge（Windows 端 grok 新探針併入 cli-install-windows 驗）、cli-detect-state、cli-install-all-providers、character-card-avatar-issues；claude-compat-endpoint 因本機無 DeepSeek／GLM／Kimi 訂閱可測，從 In progress 移回 Todo。
- stable-id-storage：儲存全面改代碼定址。主線寫實作計畫（資料格式＋四個 Rust 檔逐項規格＋14 條測試清單，見交接檔），發兩包 Opus subagent 平行實作（Rust／前端），回來主線驗證＋收尾精簡（刪掉沒人呼叫的 rename_character 與重複檢查）。落地：`worlds/<ULID>/characters/<ULID>.md`，顯示名降級成檔案內欄位（state.json 的 name／卡片 frontmatter 的 name），劇情事件存 `speaker_id`＋`speaker_name` 快照，生成圖庫改到世界目錄內（順手修掉放錯層 bug），同名角色與含 `/` 的桌名全部放行，不寫舊資料遷移。cargo 104 綠（基線 99）＋clippy 0 error＋npm build 綠＋前後端 invoke 參數機械比對全對得上；**app 尚未實跑**，GUI 驗收留給使用者。commit 8657d5c
- stable-id-storage：GUI 實測揪出空桌回收會誤收「改過名但還沒放內容」的桌（既有行為，回收條件只看零訊息／零角色／world.md 空白，沒看名字）。改成名字還掛著自動名才回收，判斷放前端（自動名是語系字串），切桌與開新桌兩處共用。npm build 綠。
- stable-id-storage：改桌名輸入框中文選字按 Enter 會直接送出（它自己攔 Enter，不像對話框交給表單處理），加上組字中放行的判斷。npm build 綠。
- stable-id-storage：改桌名 Enter 誤送第二修——isComposing 判斷在 WKWebView 無效（實測仍直接確認），改成把輸入框包成表單、Enter 交給瀏覽器的表單送出（對話輸入框同款做法），form 加 display:contents 維持版面。npm build 綠。
- stable-id-storage：生圖來源下拉顯示的是 CLI 代號首字大寫（「Agy」），改成統一讀既有的 CLI_LABELS（顯示「Gemini CLI」等，與設定頁一致）。npm build 綠。
- sponsor-features：生成歷史圖庫使用者實機通過（Gemini CLI 生圖→縮圖列→套用到卡片），在 stable-id-storage 改代碼定址後複驗；併驗項目結束，交接檔 Verification 補上。
- stable-id-storage 結案：使用者實機驗收全通過（同名角色各自點名回對的內容、刪一張另一張完好、改名後圖與圖庫都在且舊對話仍顯示舊名、桌名含 `/`）。實測揪出三件並修掉：空桌誤回收改過名的桌、改桌名中文選字誤送、生圖來源顯示「Agy」。交接檔進 archive/，任務行搬 DONE.md。
- release-4-theme-pack：使用者要求把「贊助解鎖檔案」提前到發佈產線之前，Next action 改寫並補上與 sponsor-features（手改旗標暫代、匯入入口未做）、theme-pack-component-skins（預留元件裝飾 schema）的相依。
- player-card：新任務立案——玩家角色卡（名字＋社會身份，NPC 能叫玩家名字）。設計拍板：只有公開面欄位、掛 GM 卡正下方固定位、每桌一張、沒設卡完全維持現狀；點名哨兵撞名為最大陷阱。尚未動工，交接給下一手（額度收尾）。
- player-card：玩家角色卡前後端實作完成。存檔格式拍板＝玩家卡就是一張存在該桌 characters/ 下的角色卡、id 記在 state.json 的 player_card_id，list_characters 濾掉它（側欄／GM 登場角色／點名名單一次全乾淨），後端零新命令，頭像／上傳圖／大圖窗／AI 生圖全部沿用角色卡既有機制。提示詞注入玩家身份、點名哨兵把玩家名字映射回「玩家」、逐字稿改用玩家名字；側欄 GM 卡正下方掛卡（與 GM 共用皮革樣式，沒建卡時是虛線空位卡）。cargo test 106 綠、npm build 綠、clippy 無新增警告。GUI 未實跑，等使用者實機驗收四項。
- player-card：使用者實機看過後的 UI 調整四項（玩家卡名稱欄改玩家口吻、玩家卡皮革混白 18% 與 GM 卡區分、composer 送出鈕移到最左避免誤按「請某某發言」、卡片編輯畫面頂部切兩塊讓圖片與圖片鈕靠右且與按鈕列齊高）。commit 37ab0dc／e104d9b／bdf8795／69e6f11／28ea4d1，npm build 全綠。
- player-card 結案：使用者實機建了玩家卡「布奇」並把版面調到滿意。同輪 UI 收尾：有圖時隱藏 emoji 圖示欄、編輯畫面分界定在 40%（圖片與圖片鈕靠分界左對齊，圖落畫面中央）。commit f6b865a／c032e8c／05947ff／2f45c0b。交接檔進 archive/，任務行搬 DONE.md；GM 點名輪到玩家的實機路徑未見紀錄，已在 archive 檔標明。
- player-card 結案後小修：玩家代號從中文「玩家」改成 `__PLAYER__`（人名不可能撞到），GM 點名回覆吐代號／「玩家」／「Player」／玩家卡名字都對回代號；前端比對改用常數。cargo test 106 綠、npm build 綠。
- player-card 收尾驗證：使用者實機確認 GM 推進點到玩家會停下並顯示「GM 請「布奇」發言」（先前畫面上的 `__PLAYER__` 是前端未重建時留下的舊紀錄）。archive 檔的「未驗一項」改為已驗證。commit 0e9a7b4／d98dd46。
- release-4-theme-pack 提前段：贊助解鎖檔案（`.ttpack`）完成。格式＝單一 JSON（type＋format 兩必要欄位，榮譽制不驗簽章）；app 啟動掃「文件/TableTavern」任何 `*.ttpack` 或作者頁匯入即解鎖，`preferences.sponsor_unlocked` 手改旗標全數移除。cargo test 114 綠（+4）、clippy 0 error、npm build 綠、舊旗標 grep 0 命中。販售檔已產出在 ~/Desktop/TableTavern-SponsorPack.ttpack（不進 repo）。實作外包 codex（gpt-5.6-terra），主線審 diff＋本機複驗。
- release-4-theme-pack：使用者實機驗收匯入 `.ttpack` 成功解鎖，交接檔與任務行更新。Ko-fi 商品頁連結替換等開店後（使用者屆時交 opus）。
- README 改版：改寫成玩家導向雙語（README.md 英文＋README.zh-TW.md 繁中，互連），新增 Q&A 八題含「測試期間請不必贊助」；工程內容縮成一節。
- ai-error-messages：新任務立案並實作完成。AI 失敗訊息分兩類人話化（額度用完／未登入），認不出來走保底原文＋原始錯誤一律留小字。拍板不做自動換來源（花玩家自己帳號的錢，決定權留玩家）。npm build 綠、13 例真實錯誤樣本零誤判，等實機看一眼。
- ai-error-messages 結案：使用者實機確認額度用完與未登入兩類訊息都正常顯示（commit 957b9eb）。交接檔進 archive/，任務行搬 DONE.md。
- 打包（使用者下令）：Mac DMG `Table Tavern_0.2.0_aarch64.dmg`（4.7MB，adhoc,runtime 簽章）＋Windows CI [run 30330974311](https://github.com/TaoGongSun/Table-Tavern/actions/runs/30330974311) success，artifact `table-tavern-windows-unsigned`（8.2MB）。首次 Mac 打包失敗於 bundle_dmg.sh，真因是上一輪打包殘留的暫存掛載卷 /Volumes/dmg.*，卸載後重打即過。
- codex 生圖修復：Codex 一回合回多則訊息（前導說明＋路徑），舊解析直接串接，路徑被前一句黏成「…浮水印。/Users/…png」而讀不到檔，UI 顯示「回覆中沒有圖片」——圖其實早就生出來躺在 `~/.codex/generated_images/`。parse_codex_line 每則訊息補換行、extract_image_refs 改回傳候選清單由呼叫端挑存在的檔（並補「從斜線起算」的切法救黏字）。拿當天兩份真實 Codex 輸出回放：舊邏輯都抓到壞路徑、新邏輯都抓到存在的檔；cargo test 115 綠。生圖等待提示 10–60 秒改 1–2 分鐘（實測約 2 分鐘）。commit 10f2545
- grok 生圖查證：不是程式問題，是 xAI 帳號生圖被擋——`image_gen` 回 `HTTP 403 personal-team-blocked:spending-limit`（需加值或訂閱），Grok 照約定回 NO_IMAGE。聊天額度與生圖額度分開計費，聊天正常不代表能生圖。不代玩家花錢，只給資訊。
- cli-image：收到 NO_IMAGE 改顯示「生圖額度不足或方案沒包含生圖」；Claude Code CLI 選為生圖來源直接說它不會生圖並停用生成鈕（聊天來源是 claude 時生圖視窗預設退回 API）。cargo test 115 綠、tsc 綠。commit 927fdc2
- macos-permissions：查清「Table Tavern 想取用你的桌面／音樂」的真因——tccd 日誌 `accessing=com.anthropic.claude-code`、`requesting=com.apple.sandboxd`、`responsible=com.tabletavern.app`，即 Claude Code CLI 啟動時自建 macOS 沙盒，系統把要求掛在發動它的 app 名下。我們的程式沒碰那些資料夾（給的是 `--tools ""`）。`CLAUDE_CODE_SANDBOXED=1` 未能擋掉（打包版實測仍要媒體資料庫，14:45:48 那筆歸屬仍是 claude-code）。改走說明路線：Info.plist ＋ `.lproj` 補文件／音樂／桌面三句說明，彈窗語言跟系統語言走。打包驗證三鍵入包、中英文字串正確、`codesign --verify --deep --strict` 通過。commit 4a2332a
- macos-permissions 續：補下載項目、網路卷、外接裝置三句說明（commit 7e1f267）。接著試「把責任歸屬還給 CLI」（posix_spawn 的 disclaim，自建管線）——機制實測有效（新增測試專盯子行程責任人是它自己，cargo 116 綠），但**結果更糟**：Claude Code 是裸執行檔、檔名即版本號，彈窗變成「2.1.210 想要取用 Apple Music…」，沒名字沒說明，比掛我們的名字更可疑。實作 7da2b55、回退 f1aab1c，程式碼留在歷史裡，等 Claude Code 改成 .app 包再撿。全案查因、三條路的取捨與重看時機寫在 [.ai/reference/MACOS-CLI-PERMISSION-PROMPTS.md](../reference/MACOS-CLI-PERMISSION-PROMPTS.md)。
- cli-permission-notice：改採「說明清楚」路線的第二層——app 內先預告。每家 CLI 首次啟用（設定頁按儲存、CLI 還沒被叫起來）彈一次、設定頁常駐同一段供事後查、生圖對話框選到 CLI 來源時也顯示；只記每家一次不記版本。文案經 Sol 覆核後修掉「AI 工具要的」這種會被讀成必要權限的講法，再依回退結果改成講 Table Tavern 自己的名字，最後用詞從 macOS 改成「系統」以免平台分岔（平台判斷因此整段拿掉）。commit a04351c／67ac8bf／3e89b14／3d1ed87，tsc 綠、npm build 綠、cargo 115 綠。
- 打包（使用者下令）：Mac DMG 重出，簽章 adhoc,runtime 驗證通過，六句權限說明確認入包。等使用者實機走一次「設定頁選 CLI → 預告 → 系統彈窗」全流程。
