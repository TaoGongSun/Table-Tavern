- 00:10 +0800 — `progress` — `sample-world-i18n`：使用者拍板首開先選語言（下拉）後開工，程式碼完成（主線 Fable 5 直做）：create_sample_world(root, lang) 產 zh-TW／en 範例桌（Fox/Knight/Bard＋Mistmouth 世界書＋英文開場旁白）、前端 FirstRun 畫面（預選系統語系、即選即換介面語）、無桌但語言已設則直接建桌不再問；cargo test 41 綠、npm build 綠。剩使用者模擬首開實測
- 00:10 +0800 — `created` — `i18n-more-languages`：使用者提議介面擴十國語言（AI 產字典）入列待辦，需先定驗證流程
- 00:40 +0800 — `done` — `ui-settings-panel`：使用者複驗五檔文字大小通過（附截圖），結案
- 00:40 +0800 — `chore` — `sample-world-i18n`：應使用者要求刪首開測試資料（~/Documents/TableTavern/worlds 整目錄＋config 的 language/last_world 鍵，AI 連線設定保留），待使用者實測首開流程。另發現 Documents/TableTavern 下有 worlds/ 之外的舊孤兒目錄 TestWorld、獸肉，未動待確認
- 01:00 +0800 — `done` — `sample-world-i18n`：使用者實測首開通過（語言選擇→English→英文範例桌，附截圖），結案。另應使用者確認刪除 Documents/TableTavern 下舊孤兒目錄 TestWorld、獸肉
- 01:50 +0800 — `progress` — `post-mvp-scene-summary`：前後端完成（Opus subagent 實作、主線規格＋驗收）：transport summary_messages（只壓公開 transcript，防私有外洩）＋data begin_next_scene（【前情提要】/Previously: 旁白落新場景開頭、鏈式壓縮）＋lib advance_scene（空場景擋、GM 檔位）＋前端換場鈕；主線親跑 cargo test 43 綠、npm build 綠、行號抽查。剩使用者實測
- 12:30 +0800 — `done` — `post-mvp-scene-summary`：使用者實測換場通過（附截圖），結案
- 12:35 +0800 — `progress` — 小修：玩家輸入框 placeholder 改對象在前「對「{name}」發言（玩家身分）…」（原寫法對象被截斷），主線直改（幾行字串不值得發包），npm build 綠
- 12:40 +0800 — `created` — `scene-history-browser`：使用者提議拍板（過去的場收合列表＋單場檢視與匯出＋紀錄過長建議換場提醒），已發包 Opus subagent 實作
- 13:10 +0800 — `progress` — `scene-history-browser`：前後端完成（Opus subagent 實作、主線規格＋驗收）：data export_scene_markdown（共用渲染 helper、整桌版行為不變）＋lib export_scene command＋前端 SceneViewer modal／過去的場收合列／8000 字元換場提醒；主線親跑 cargo test 45 綠、npm build 綠、行號抽查。剩使用者視覺驗收
- 14:00 +0800 — `progress` — `scene-history-browser`：使用者驗收回饋擴充為「主欄閱讀優先」改版（NewPlan §9.4 增拍板：訊息上方只留 header 一行；幕用語；前幕浮層；單幕整面閱讀停用發言；角色卡編輯上卡；世界設定暫掛 GM 卡）。NewPlan 376 行首開描述原地改寫＋§9.4 加一條（568→569 行）。已發包 Opus subagent
- 15:00 +0800 — `progress` — `scene-history-browser`：主欄閱讀優先改版完成（Opus subagent 實作＋中途接收使用者修訂改整面編輯；主線規格＋驗收）：幕用語、header 右排三鈕、前幕浮層雙收法、mainView 三選一整面取代（閱讀/卡編輯/世界設定，皆停用發言）、卡上 ✎ 鈕＋卡面圖、GM 卡置頂暫代世界設定；主線親跑 npm build 綠、cargo test 45 綠、逐需求行號抽查。剩使用者視覺驗收
- 16:10 +0800 — `progress` — `scene-history-browser`：二輪修訂完成（同一 Opus subagent 續作、主線驗收）：換幕取幕名（extract_scene_title 純函式＋WorldState.scene_titles＋清單「第 N 幕：幕名」）、前幕改右側捲軸面板（隱藏鈕置頂區隔）、✎ 靠右、textarea 限縱向拉伸；主線親跑 cargo test 47 綠、npm build 綠、抽查行號。剩使用者視覺驗收
- 16:40 +0800 — `progress` — `scene-history-browser`：使用者回饋「更小字級時主欄兩側空白過大」→ 移除 .chat-main 的 max-width: 46rem（rem 定寬隨字級縮水是根因），主欄自動填滿左右；主線直改（一行 CSS），npm build 綠
- 17:00 +0800 — `progress` — `scene-history-browser`：使用者回饋右側捲軸常駐→根因是 body 預設 margin 8px 使 100vh app-shell 恆溢出 16px，補 body{margin:0} reset；此後只有內容真的超出（訊息多或視窗拉短）才在對話區出現捲軸。主線直改一行 CSS，npm build 綠
- 17:15 +0800 — `progress` — 小修：composer placeholder 刪（玩家身分）註記，只留「對「{name}」發言…」（en: Speak to “{name}”…），主線直改，npm build 綠
- 17:30 +0800 — `progress` — 小修：編輯畫面「返回」從頂部標題列移到「儲存角色卡」／「儲存世界設定」右邊（EditPane 卸下 onBack、由 CardEditor/WorldEditor 表單自渲染），主線直改，npm build 綠
- 18:00 +0800 — `done` — `scene-history-browser`：使用者驗收通過（幕名／右側前幕面板／整面閱讀與編輯／主欄填滿／捲軸修復／返回鈕位置），結案。今日累計結案：post-mvp-scene-summary、scene-history-browser
- 11:04 +0800 — `progress` — `post-mvp-character-archive`：拍板 frontmatter `archived` 旗標（不搬目錄），完成收起／還原／真刪除（.md＋.png）＋plugin-dialog 確認框；GM assemble 與點名過濾隱藏角色；i18n 雙語。實作外包 codex（gpt-5.6-sol，經 codex-rescue subagent），主線架構拍板＋親讀 diff 複驗：cargo test 48/48、npm build 綠。剩使用者實測三條路徑
- 12:07 +0800 — `progress` — `worldbook-st-format`：世界書 v2 全功能落地。研究（ST 31 欄位格式、character_book V2/V3、可見性缺口）外包 haiku subagent＋主線親讀原文核驗；拍板 worldbook.json 原樣保存只解讀子集、可見性走 extensions.table_tavern.visibility、觸發 v1＝constant＋近 4 則關鍵字。R1 後端＋R2 前端皆 Codex gpt-5.6-sol 實作、主線親讀 diff 複驗：cargo test 55/55、npm build 綠。剩使用者實測
- 12:27 +0800 — `progress` — `worldbook-st-format`：使用者實測匯入與條目編輯 OK；回饋「點 GM 卡直接開世界設定，不要鉛筆鈕」→ GM 卡整卡可點（含鍵盤操作）、移除鉛筆鈕、游標改 pointer。主線直改兩檔，npm build 綠
- 12:29 +0800 — `progress` — `worldbook-st-format`：使用者回饋恆定欄不易懂→編輯表單的「恆定」checkbox 改帶說明文案（不用等關鍵字，每次發言都送進上下文），清單 badge 維持短版；i18n 雙語，主線直改，npm build 綠
- 12:48 +0800 — `done` — `post-mvp-character-archive`：使用者實測收起／還原／刪除確認框全通過，結案
- 12:48 +0800 — `progress` — `worldbook-st-format`：R3 使用者回饋（新增條目要置頂＋條目可上下移動）→ 沿 ST 語意用 displayIndex 管顯示順序（order 注入優先權不動）：新增置頂、↑↓ 移動、舊檔正規化。Codex gpt-5.6-sol 實作、主線親讀 diff 複驗 cargo 60/60、npm build 綠
- 12:52 +0800 — `progress` — `worldbook-st-format`：使用者回饋世界書開著時還原角色、指定角色清單不會更新→點「指定角色」radio 當下重抓 list_characters（過濾 archived）。主線直改一處，npm build 綠
- 12:57 +0800 — `progress` — `worldbook-st-format`：使用者回饋要 Discord 式未儲存提示→世界設定畫面追蹤未儲存數（world.md 文字＋開啟中的條目表單快照比對），按鈕列右緣琥珀色「有 N 項修改未儲存」，返回時彈確認框防丟失；i18n 雙語。主線直改三檔，npm build 綠
- 13:11 +0800 — `done` — `worldbook-st-format`：使用者實測全數通過結案。核心驗收：資訊邊界實聊——指定角色 Knight 答得出條目內容、未指定 Fox 不知情；另含匯入、CRUD、置頂／移動、未儲存提示、五輪 UI 回饋修訂。今日累計結案：post-mvp-character-archive、worldbook-st-format
- 13:24 +0800 — `progress` — `post-mvp-more-cli-providers`：開工。拍板：本輪只做 gemini 端到端＋一鍵安裝（可見終端機、OAuth 回跳 CLI、app 不碰 token），取代舊「不代裝」約束；發現使用者機上 agy 實為 Antigravity CLI 非 gemini-cli，兩套已反安裝備妥測試機（agy→agy.bak、brew uninstall gemini-cli）。研究外包 haiku subagent＋主線抽查原始碼：settings 欄位 security.auth.selectedType=oauth-personal、憑證檔 ~/.gemini/oauth_creds.json、headless -p/-m/--output-format、NO_BROWSER 替代流程、最新版 v0.52.0
- 13:45 +0800 — `progress` — `post-mvp-more-cli-providers`：轉向確認——gemini-cli 個人帳號已被 Google 停用（IneligibleTierError，主線實裝實測證實），目標改 Antigravity CLI（agy）。主線本機實測 agy 介面（-p 必帶值、stdin 不進上下文、純文字輸出、agy models 清單、headless 工具自動拒絕），Codex gpt-5.6-sol 原地改寫接入（gemini 碼全刪），主線複驗 cargo 62/62、npm build 綠、gemini 符號清零。安裝研究（haiku）：官方 curl 一鍵腳本、agy update、免登入訊號改用功能探針。lessons 記兩筆：agy stdin 假設推翻、sol 升檔判準修正
- 13:51 +0800 — `progress` — `post-mvp-more-cli-providers`：一鍵安裝完成（Codex gpt-5.6-terra 實作、主線親讀複驗 cargo 64/64、npm build 綠）：設定頁 agy 未偵測時出「一鍵安裝」鈕→開可見終端機跑官方 curl 腳本→功能探針等 OAuth 完成→印「驗證成功，已連結，可以關閉終端機視窗」；app 每 3 秒偵測、10 分鐘上限、卸載清理。剩本機實測（使用者在場時跑）
- 14:00 +0800 — `progress` — `post-mvp-more-cli-providers`：顯示名改「Gemini CLI」（fe9780d）；使用者本機實測一鍵安裝通過（agy 搬走→設定頁按鈕→終端機腳本重裝→探針通過，證據：~/.local/bin/agy mtime 13:57 重建、1.1.6 回應正常）。另修正本檔 12 筆腦補時間戳（照 commit 實時），lessons 記一筆
- 14:10 +0800 — `progress` — `post-mvp-more-cli-providers`：使用者實聊驗收 agy 供應商通過（回覆速度符合 flash 特徵）；grok 接入開工——主線本機實測 0.2.111：-p 單發、streaming-json 事件（thought/text/end）、grok models 清單、不帶 --deny "*" 會真的執行工具（實測跑了 ls）故旗標全關工具；接入實作已發包 codex terra
- 14:13 +0800 — `progress` — `post-mvp-more-cli-providers`：grok 供應商接入完成（codex terra 實作、主線親讀複驗 cargo 67/67、npm build 綠）：偵測、-p 單發＋工具全關旗標組、thought/text/end 解析、grok models 動態清單、設定頁 Grok CLI 選項。剩使用者 app 內實聊 grok 即整題結案
- 14:18 +0800 — `done` — `post-mvp-more-cli-providers`：使用者實測 grok 實聊通過（Knight 發言），三路驗收全過結案；另立 todo：cli-custom-provider（自訂 CLI 供應商構想已寫入任務檔）。今日累計結案：post-mvp-character-archive、worldbook-st-format、post-mvp-more-cli-providers
- 14:23 +0800 — `progress` — 開新任務 test-build-cross-platform（測試版打包 Mac ad-hoc 簽章＋Windows CI）：主線查證機上零簽章憑證、Pirahus 可跑＝ad-hoc 路線可行；細節與開工指引寫入任務檔，留給新對話執行
- 14:45 +0800 — `progress` — `test-build-cross-platform`：Mac DMG 出爐（4.5MB，codesign adhoc,runtime、乾淨路徑掛載＋啟動煙測通過）；新增 .github/workflows/test-build.yml，CI run 30072316789 成功，artifact 含 .msi＋.exe（commit 9cb2a37）。剩兩平台實機驗收
- cli-install-all-providers：四家 CLI 一鍵安裝實作完成（codex 發包＋主線修 PATH/文案），cargo 70 綠＋npm build 綠；另整理 README＋新建 CHANGELOG；刪除舊測試打包產物（Mac DMG＋CI artifact）。
- cli-install-all-providers：使用者實測 codex 一鍵安裝抓到探針 bug（exec 非 git 目錄拒跑致成功訊息不出現），改 codex login status（ce64cf2）。
