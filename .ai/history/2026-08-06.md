- prompt-cache-optimization：額度分頁改成「省下多少」口徑（使用者要求減額度焦慮）。第一眼＝「已省 X% 費用」＋「約省下 $Y」，總 token 與總花費退到細項（細項的花費欄維持不動——保溫 ping 那列沒有「省下」可言，改掉會讓該列與總計都變得莫名其妙，使用者看畫面後回頭修正）。金額不建價目表——拿各 CLI 回報的該輪 cost_usd 反推輸入單價再乘回省下的 token；快取係數只列 claude／codex／grok 三家，agy 與 api 估不出就整列標 partial，只要湊不齊第一眼就不出金額。cargo 326／vitest 22／build／i18n 全綠；使用者實機看過（那桌 811,962 tokens、命中 63% → 已省 51%、約省下 $1.968），唯一回饋是右上金額加「省下」二字消除歧義，已改。
- ai-card-refactor：**七包規格定案，寫進 [CARD-REFACTOR-SPEC.md](../reference/CARD-REFACTOR-SPEC.md)**（本對話只討論＋寫規格，不實作）。起點是使用者找不到機制帳本——釐清帳本是世界書分頁的折疊區、只在有記錄時出現，30 幾桌只有迷宮之主那桌有（4 筆已接管、0 跳過），西幻卡零硬標記所以本來就空。由此發現原入口「帳本非空才問」會漏掉整型純散文卡，改成**每張卡都能重構**；使用者再補上 Chub 主流是多角色世界書卡的觀察，人物合集拆成角色卡納入產物（西幻卡實據：七國守護者 7 人／傳奇冒險者 4 人／重要 NPC 4 人擠在三條 constant 條目裡，共 1786 字元）。逐包拍板：①一顆按鈕全包、人審雙軌（一鍵全套用＋展開逐項勾選，預設全勾）②兩階段呼叫（盤點→逐條展開）、脈絡放 system 吃快取、產物用玩家介面語言（拆卡順帶翻譯）③沒勾的人一人切一條標記人物、原條目留總述、收據要存被改條目原文 ④在場過濾＋角色自動上下場結合封存機制：幕中只 append 進歷史（動 system 會炸快取）、換幕才結算 archived、封存拆自動隱藏／手動封存兩態 ⑤渲染三層（自帶殼＞AI 產 HTML 進沙盒＞通用元件保底），失敗時建議換模型重構但不強制 ⑥機制殘渣抽欄位規則＋觸發表 ⑦算術求值器（約 250 行，derived／Roll／ttrpg 骰池三處共用）。卡型分佈前置同步完成（零額度）。memory 新增 player-dual-path（玩家面一鍵＋可展開逐項，架構面才細緻保險）。未 commit。
- vn-cg-generation：立案（只查證＋立案，不實作）——查明 ST 玩家用 NAI 生圖的做法：Image Generation 擴充選 NovelAI Diffusion，提示詞由玩家自己的聊天 LLM 把劇情轉成 Danbooru 標籤（所以「不用自己寫」＝多燒一次文字呼叫），Auto Illustrator 類擴充再把圖就地插進對話。與我方差距：引擎（NAI 動漫特化 vs `google/gemini-3.1-flash-image` 通用）、提示詞（標籤 vs 整段中文散文直送）、成本（NAI Opus $25/月一般尺寸不扣 Anlas＝實質吃到飽 vs 按張計費）、畫風控制（負面提示／seed／角色專屬前綴我方全無）。使用者拍板列為長期可考慮，動機鎖定 VN 模式 CG 即時生成（免作者備圖庫、無圖床死穴）。未 commit。
- card-import-flow：兩件。**（一）雙世界書由硬擋改成可融合**（推翻拍板 4 後半，使用者實機驗收通過）——起因是使用者拿兩本 furry 世界書問「適不適合融合」，拆檔後發現關鍵字零碰撞（89 vs 119 個，交集 0）、書級設定全同，是互補的兩層（20 條種族身體規格 vs 67 條名詞小字典），唯一地雷是 Anthro Anatomy 有 12 條 constant（約 4.9 萬字）且 app 沒實作 `token_budget`。查證後確認資料層本來就會合併去重、收據也撐得住兩筆，封鎖純粹在前端藏按鈕。路由名與 i18n 鍵 `block_double_worldbook`／`importRouteBlock*` 一併改成 `merge_worldbook`／`importRouteMerge*`（舊名已成假話），新增「仍要匯入」十語系。build／vitest 22／check:i18n 十語系全綠。**（二）身分判定改版拍板**——同批的兩張 `furry-male-scenarios` 卡內容全是世界書卻塞在 description、`character_book` 空，現行分流直接判成角色卡連問都不問，29／10 個開場白因此被鎖住（開場白選單只走世界書路徑）。跑全 TestCards 統計定判準：`first_mes` 18／18 全有值不可用，`alternate_greetings` 只 4 張有且零誤判。拍板三步（人設欄→條目轉換／判準改主按鈕／身分框全開，例外只留 `!name || book_shaped`），順序不可調換——不先接轉換路，主按鈕會指向一條報 `missing entries` 的死路。
- card-import-flow：**身分判定改版三步實作完成**（承同日拍板）。(1) `worldbook_json` 從「只剝 character_book」改成三段：條目非空剝那層／頂層有 entries 原樣過／兩者皆無走新的 `persona_as_worldbook()` 把非空人設欄併成一條沒關鍵字的常駐條目；人設欄清單抽 `PERSONA_FIELDS` 與 `lorebook_heavy` 共用（原本內嵌重複），刻意排除 `first_mes`（開場白走 card_openings，收進條目會每回合重複注入），有條目的卡完全不動以免回歸既有驗收。(2) probe 加回 `alternate_greetings`（判準用途）。(3) 前端分流三條縮成兩條——只有 `!name || book_shaped` 維持零詢問直匯，其餘一律跳身分框，判準抽成 `looksLikeWorldbook()` 只決定主按鈕與文案；`importChoice` 狀態由整包 probe 瘦成 `booksFirst`。零新 i18n 字串。cargo 327（+1）／build／vitest 22／i18n 九語系全綠；真卡端到端實跑：兩張 furry-male-scenarios 各匯出 1 條 1882／1895 字常駐條目、開場白 30／11 條未被吞。已知待觀察：有匯入紀錄的桌會連跳兩個框。
- card-import-flow：**收據為空時的條目保險**（實機驗收挖出）。純世界書檔匯進「有內容但沒收據」的舊桌不跳確認框，第二本書無聲合進去。第一版修法把「桌上有沒有條目」擺在最前面蓋過收據，被使用者當場糾正——**收據為主、條目最多當保險**，不為測試期間的意外改寫長期判準（等所有桌都有收據後那格自然不再觸發）。改回收據決策表原樣，只在 `receiptKinds` 為空那一格加條目查詢，且只管世界書身分。測試特地釘一條反向樁：角色卡帶進來的條目不會把配套世界書從 companion 變成要問。build ✓／vitest 23（+1 組）／i18n ✓，Rust 未動。
- card-import-flow：**角色卡路徑也給開場白**。追查「football-team 有開場白為何判成角色卡」時釐清兩個都叫「開場白」的機制其實不同：主按鈕判準只看 `alternate_greetings`（football-team 為 0），匯完跳不跳選單看 `first_mes`＋`alternate_greetings` 相加且只在世界書路徑跑。由此挖出真缺口——角色卡的 `first_mes` 匯完只躺在卡的「### 開場白」欄位（備用開場白落私有筆記），**沒有任何地方會貼它**，設計註解寫的「開場白已在卡上不必再貼」與實情不符。使用者拍板：角色卡路徑也呼叫同一個 `offerOpeningLine`，**貼出形式維持旁白不改成角色發言**（開場白常是場景或角色的描寫，不是那個角色說的話）；判準維持不動，「開場白總數 ≥2」的統一提案作廢。build ✓／vitest 23／i18n ✓，淨 +4 −3 行。實機驗收 (a)(b)(c) 通過。
- card-import-flow：**收工**。今日四件（雙世界書可融合 a73ca94／身分判定改版三步 26f6def／收據為空時的條目保險 83a9e3a／角色卡路徑也給開場白 d6688fb）全數實機驗收通過。最終自驗 cargo 327／build／vitest 23／i18n 九語系全綠。交接檔 80 行瘦身成 33 行，已驗收明細搬 [archive/card-import-flow-completed.md](../handoffs/archive/card-import-flow-completed.md)。任務未結案——最初驗收清單第 4（復原連按逐筆退）、7（配套世界書 companion）還沒跑，但當初卡住的原因（缺樣本卡）已解除，配方寫進交接檔 Next action：第 7 項用「塞拉菲·内藤（零條目角色卡）＋Anthro Anatomy」就能湊出 companion 情境。使用者補充產品立場：角色卡主流是一桌只玩一張，預設多角色反而反主流，所以「每張角色卡都跳開場白面板」不算煩（已存 memory `single-character-is-mainstream`）。
