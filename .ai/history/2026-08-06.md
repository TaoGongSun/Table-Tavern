- prompt-cache-optimization：額度分頁改成「省下多少」口徑（使用者要求減額度焦慮）。第一眼＝「已省 X% 費用」＋「約省下 $Y」，總 token 與總花費退到細項，細項「花費」欄改「已省金額」。金額不建價目表——拿各 CLI 回報的該輪 cost_usd 反推輸入單價再乘回省下的 token；快取係數只列 claude／codex／grok 三家，agy 與 api 估不出就整列標 partial，只要湊不齊第一眼就不出金額。cargo 326／vitest 22／build／i18n 全綠；使用者實機看過（那桌 811,962 tokens、命中 63% → 已省 51%、約省下 $1.968），唯一回饋是右上金額加「省下」二字消除歧義，已改。
