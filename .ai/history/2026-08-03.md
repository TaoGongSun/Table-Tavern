# 2026-08-03

- prompt-cache-optimization：與使用者在 Cowork 對話分析提示詞快取後開新任務——現況 stream_chat 無 cache_control（Claude 系經 OpenRouter 命中恆 0），keyword 世界書條目與 GM「目前狀態」拼在 system prompt 系統性打破前綴；拍板三塊依 A 穩定前綴重構→C 命中率量測→B Claude 顯式斷點。純分析未動程式，任務檔＋交接檔已建（行號證據以 HEAD f8801e8 核對）。
- prompt-cache-optimization：A 穩定前綴重構完成——assemble_messages／assemble_gm_messages 把 keyword 世界書條目與 GM「目前狀態」搬到 transcript 尾端獨立 user 訊息（不走 push_merged），constant 條目與角色卡留在 system；測試對齊＋新增快取友善驗收（連續兩輪去尾後前綴逐字相同），cargo test 164 全綠（雲端 Cowork 會話自驗）。接著實測遵循度＋做 C。
