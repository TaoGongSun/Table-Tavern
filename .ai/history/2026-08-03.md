# 2026-08-03

- prompt-cache-optimization：與使用者在 Cowork 對話分析提示詞快取後開新任務——現況 stream_chat 無 cache_control（Claude 系經 OpenRouter 命中恆 0），keyword 世界書條目與 GM「目前狀態」拼在 system prompt 系統性打破前綴；拍板三塊依 A 穩定前綴重構→C 命中率量測→B Claude 顯式斷點。純分析未動程式，任務檔＋交接檔已建（行號證據以 HEAD f8801e8 核對）。
- prompt-cache-optimization：A 穩定前綴重構完成——assemble_messages／assemble_gm_messages 把 keyword 世界書條目與 GM「目前狀態」搬到 transcript 尾端獨立 user 訊息（不走 push_merged），constant 條目與角色卡留在 system；測試對齊＋新增快取友善驗收（連續兩輪去尾後前綴逐字相同），cargo test 164 全綠（雲端 Cowork 會話自驗）。接著實測遵循度＋做 C。（commit dc95f44）
- prompt-cache-optimization：C 命中率量測完成——stream_chat 對 OpenRouter 端點加 usage accounting（chat_request_body 抽出、非 OpenRouter 請求形狀不變）、extract_usage 解析 SSE 尾塊 cached_tokens、串流結束 eprintln 一行 [prompt-cache] 命中率；查證修正：OpenRouter 不回報快取寫入 token 數。cargo test 167 全綠（162＋A 2＋C 3）。剩實機驗收與 B。（commit 601ce51）
- prompt-cache-optimization：B Claude 顯式斷點完成（使用者拍板現在做）——anthropic/ 系模型請求序列化時 content 轉 multipart、標兩個 ephemeral 斷點（system＋最後一則 assistant，靠 Anthropic 約 20 block 回看逐輪增量命中）；不改 ChatMessage 型別，非 anthropic 模型逐位元不變。cargo test 168 全綠。三塊收工，剩實機驗收＋命中率 UI 拍板。（commit 7ea85d5）
- prompt-cache-optimization：命中率落檔（拍板：不做正式 UI）——stream_chat 加 usage_log 參數，逐行 append 到文件/TableTavern/prompt-cache.log（時間戳＋model＋token 數＋hit_rate，寫檔失敗不影響聊天），stderr 行保留；測試擴充驗落檔內容，cargo test 168 全綠。程式面全部收工，唯一剩餘：實機驗收。
- 交接檔盤點（Cowork 對話，未動程式）：修正 `.ai/TASKS.md` 兩處與交接檔不符的待拍板狀態——sponsor-features 的「提示詞標籤」已於 07-30 以構圖二選一結案（只剩 Ko-fi 導購歧義）、i18n-more-languages 的四件已於 07-30 全數拍板。
- 任務關閉（使用者拍板）：**release-4-theme-pack** 主題載入引擎／自選桌布／AI 產生主題不做，五套配色維持寫死在 App.css，NewPlan §16.1 同步註記縮減 scope，交接檔封存 `handoffs/archive/`；連帶關閉 **theme-pack-component-skins**（無主題檔格式可掛元件裝飾 schema）。**cli-auto-connect** 不做（一鍵安裝已覆蓋最痛的安裝段、登入為一次性動作、風險告知不可自動化繞過），前置查證未執行即關閉。三檔 Status 改 completed 並搬進 DONE.md（25 → 28 項）。
- sponsor-features 議程清空：「Ko-fi 導購歧義」登記結案（贊助鈕早在 07-30 就已改指 Ko-fi 商品頁 `ko-fi.com/s/027754730c`，三處導購入口皆直達結帳，不再經過抖內頁；app 內解鎖說明文案併入 release-3-kofi，抖內誤購走個案處理不立政策）；順帶把「色情詞句導致生圖失敗」也標為 08-01 已由生圖失敗訊息分流（NO_IMAGE／REFUSED 暗號＋拒絕字樣保底）落地結案。本任務唯一剩餘＝實機驗收。
- cli-install-windows 交接更正：原記「07-31 兩輪修正尚未進包，等 CI verify→重打包」有誤。逐一核對 `git merge-base --is-ancestor`——`7158e65`（鎖檔提示）進 07-31 00:42 重打（`b74bbb0`）、`9a17562`（系統代理下傳）進 07-31 01:48 重打（`5586d55`），之後每次重打皆含，最新包 `c16c92e`（08-02 02:32，HEAD `ffd447b`）兩項齊備。不需重打包，唯一剩餘＝把現成包交給 Grok 回報者跑「只開系統代理」的安裝→登入→聊天全流程。任務檔、交接檔、TASKS.md 三處同步更正。
- cli-install-windows 結案：Grok 回報者回報確認連上，Windows 一鍵安裝→登入→聊天全流程通過（系統代理下傳同時獲證，代理有真的傳進聊天子程序）。Status 改 completed、移出 In progress 進 DONE.md（28 → 29 項），交接檔封存 `handoffs/archive/cli-install-windows.md` 並加結案標記。Stage C（金絲雀排程、診斷打包鈕、mac 收編引擎）與「安裝階段是否也開可見視窗」留待日後拍板，未納入結案。
- st-ecosystem-upgrades：狀態列改成條件顯示（使用者要求）——`data::world_has_state_bar` 掃 world.md／世界書啟用條目／全部角色卡，命中十一個狀態列格式詞（比對詞對齊 `extract_state_block` 認得的包裹）才回真；前端 `hasStateBar` 在切桌、匯入卡、離開世界設定時重問，沒有就整條 `.state-bar` 不掛載（看不到也點不開）。cargo test 174 綠（＋2），tsc／npm test／check:i18n 全綠；拿實機九桌跑判定（三桌顯示、四桌隱藏）並經使用者實機驗收。（commit 44c01e2）
- prompt-cache-optimization：D CLI 路徑量測完成（起因：使用者用 CLI 模式跑劇情後 log 一行都沒有，因為 A／B／C 三塊與量測全掛在 API 那條，CLI 走 flatten_messages 完全不經過）。四支 CLI 實測冒煙查證用量欄位：claude `result`／codex `turn.completed`／grok `end` 都帶 usage，agy 吃純文字拿不到（要換 stream-json 並重寫解析，使用者拍板先做三支）。關鍵坑：各家 input_tokens 語意不同——claude／grok 不含快取部分要加總（claude 實測 input=1／cache_read=4771；grok cache_read 146304 > input 31509），codex 已含 cached_input_tokens 不可再加。CliLine 不動，usage 走獨立抽取函式＋`contains("\"usage\"")` 預檢逐行呼叫；`UsageLog` 打包落檔設定當 run_cli 一個參數；log 加 transport 欄位兩條路共用，hit_rate 收進 PromptCacheUsage。測試＋4（三支 parser 各一＋假 CLI 端到端驗落檔內容），cargo test 178 全綠（本機）。剩實機驗收。
- prompt-cache-optimization：D 實測 0% 命中＋補診斷欄位。使用者三輪（20:22／20:25／20:29）落檔正常但 hit_rate 全 0；拿該桌真實素材（system 18470 字元）直接跑 claude CLI 四輪定位，證實快取機制本身正常——第一輪不建、第二輪建 10870、第三輪讀 10840、第四輪讀 10737。已排除前綴不穩定（constant 集合與 events 無關、排序穩定、巨集只替換 {{user}}／{{char}}），主假設為三輪間隔 3～4 分鐘踩到 Anthropic 5 分鐘 TTL。第一版 log 漏了診斷關鍵欄位：只記讀快取分不出「沒建」與「建了讀不到」，補 created_tokens（claude cache_creation／codex cache_write／grok 與 OpenRouter 不回報恆 0）＋時間戳改秒級（local_timestamp_seconds 與既有分鐘級共用 local_time_parts，存檔格式不變）。cargo test 178 全綠。待使用者連續快跑三四輪驗證 TTL 假設。
- prompt-cache-optimization：包 1 session 檔操作模組完成（session_file.rs 純函式 8 支＋單測 10 條，cargo test 188 綠；規格主線寫、實作外派 codex terra、主線親讀驗收）——hash 見本 commit。
- prompt-cache-optimization：包 2 resume 流程地基完成——claude 模式三呼叫（角色對話／GM 旁白／點名）走 resume 續聊線，lanes.json 水位＋指紋對齊、回合後抹寫私設、降級鏈全通；案 C 改寫-resume 機制以 3 次 haiku 迷你呼叫實測鎖死（改檔後 read=8717 只滅尾端）。cargo test 201 綠零警告。（commit 見同名訊息）
