- 01:20 +0800 — `created`+`progress` — `ui-overhaul`：英文合作者主導 UI 改版。前置：三面向對抗式檢討（可讀性／美感／獨特性）＋五輪候選稿收斂，拍板「Emblem」設計系統（深石板＋桌遊組件卡＋playbill 對話＋wedge 名牌；規則：wedge＝名字、虛線琥珀＝機密、serif＝故事文字、控制項同高、en 按鈕 Title Case）。第一輪實作完成：App.css 全面 token 化重寫（深淺兩主題、:focus-visible 補回）、側欄 GM 壓一行＋角色卡改組件卡（圖窗染底＋wedge＋檔位寶石）、對話區 playbill＋幕書籤、composer 整寬書寫面＋目標晶片＋「送出 ➤」主要鈕、世界書可見範圍徽章改虛線琥珀、i18n 補 playerLabel＋en 23 鍵 Title Case。實作原則：不發明 UI（Say 模式／訊息內機密徽章／epithet 皆查證非既有功能而剔除）。npm build 綠；淺色實跑截圖驗證通過。待合作者實機驗收深色模式＋實聊 playbill
- 13:18 +0800 — `progress` — `cli-install-windows`：朋友回報 Gemini OAuth 出「貼認證碼」頁但無終端機可貼→根因＝codex/agy/grok 登入是隱藏背景執行（違反「安裝過程必須可見」拍板，headless 環境觸發貼碼流程）。修法：四家登入全改 `cmd /C start` 可見終端機（poll 統一 600s），整組刪 headless 撈 URL 機制（LoginMode/spawn_streaming/extract_first_url/InstallProgress.url/前端 URL 連結，net −380 行），login 文案改指向終端機視窗；順手：一鍵安裝按鈕靠右對齊。Codex terra 實作、主線親審 diff＋複驗：cargo test 73 綠、npm build 綠、殘留 grep 零命中。CI 驗證與重打包等使用者下令
- 14:0x +0800 — `progress` — `cli-install-windows`：CI 驗證輪全綠（run 30145802375）。發現「已裝未登入」死角：偵測只看執行檔、已偵測就藏一鍵安裝鈕，無任何登入入口（朋友正卡此態）。拍板：已偵測 CLI 旁常駐「登入／驗證」鈕走同一條冪等流程（否決旗標持久化版，防過期死路）。純前端小改主線直做，npm build 綠；首輪打包 artifact（run 30146328876）缺按鈕作廢，重打包
- 15:0x +0800 — `progress` — `cli-install-windows`：朋友二輪紅（認證分頁轟炸近死機＋貼碼視窗神隱）。根因實證：未登入 agy 每次執行自開 OAuth 分頁（假 HOME 重現）×5 秒探針輪詢×前端 3 秒誤判解鎖＋無併發鎖=多條輪詢疊加。使用者拍板鐵律「每按一次最多發一次認證」→引擎 v2：刪輪詢、start /WAIT 等視窗結果、失敗零探針、try_begin 併發鎖＋60s 冷卻、重按=視窗置頂（windows-sys）、mac 補冷卻。Codex terra 實作、主線修三洞（HWND 指標型別編譯級 bug、smoke 斷言、計時器疊加）：cargo 77 綠零警告、npm build 綠。續跑 CI＋重打包
