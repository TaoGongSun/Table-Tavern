# 2026-08-04

- prompt-cache-optimization：包 4 log 升級完成——log 檔改 `prompt-cache.jsonl`，用量與線動作合成一次呼叫一行 JSONL，七個診斷標籤（ok／warmup／expired／prefix-broken／no-cache／single／drop-lane）純規則判定，補花費／線別／理論可中量。cargo test 212 綠。（commit 2eb7461）
- prompt-cache-optimization：包 5 GM 旁白＋點名合併完成——`gm_narrate` 一次呼叫產旁白＋尾端「下一位：」點名行（`extract_next_speaker` 剝行、`pick_speaker` 對回角色 id），回傳 `{text, next}`；`gm_suggest_speaker` 命令與 `suggest_instruction` 刪除，前端 `gmAdvance` 迴圈改「旁白落 transcript→點名→角色接話」、`narrateOnce` 與旁白鈕共用；GM 每輪推進少打一次 CLI。lanes 旁白 echo 同步剝點名行、`ReplyEcho::None` 隨之刪除；11 語系推進鈕 hint 改述。cargo test 213 綠零警告、npm build＋check:i18n 綠。
- prompt-cache-optimization：真桌驗收兩場四輪未達標（hit_rate 0／62.3／0／0 與 0／0／0／58.8），根因鎖死＝claude CLI 2.1.210 resume 隔輪不帶快取標記（session 檔逐筆 usage＋成本反推＋乾淨環境三連發取證；app 前綴機制驗證無誤）。診斷補 `cache-skipped` 標籤與測試 2 條，cargo test 213 綠。社群 proxy 修復否決（憑證路徑＋維護債＋對症存疑）。下一步升級 CLI 重測。（commit 0b4ca49）
- prompt-cache-optimization：CLI 升 2.1.220 重測四輪（0／0／64.3／63.7，整場 38%）；模型對照實驗鎖定 bug 只咬 Sonnet 系（Opus 全命中）；官方 issue #29966（open）佐證 CLI 內部按模型硬編碼關快取；社群無修復時程；換 Opus 經價格試算不划算（輸出單價 1.67 倍蓋過快取折扣）。待拍板：等官方＋先做包 6。（未 commit，社群查證外包 haiku 子代理）
- prompt-cache-optimization：收工交接——拍板「GM 檔位暫換 Opus 跑四輪驗 app 架構」（判讀標準寫入交接檔：第 2–4 輪 diag=ok 且 hit ≥80%、不得出現 cache-skipped；測完換回原檔位），過＝包 1–5 架構驗收通過、開工包 6。（未 commit）
- prompt-cache-optimization：**Opus 四輪真桌驗收全過＝包 1–5 架構驗收通過**——warmup 建 15,323 後，2–4 輪 diag=ok、85.3／87.2／88.3%，讀到量與理論可中量每輪只差 2 個 token，零 cache-skipped；花費 $0.138→$0.063／$0.065／$0.071。Sonnet 場的爛數字全歸 CLI 官方 bug。官方 issue 不發（使用者拍板）。下一步：包 6 額度分頁 UI。
- prompt-cache-optimization：換幕提醒改標準——快取上線後換幕不再省額度（摘要＋warmup 全額 ≈ 連跑四輪），原 8000 字元門檻約第 2–3 輪就跳、正是最虧的時候；門檻提到 30000 字元，10 語系 `sceneTooLongHint` 文案理由改成「模型顧不上前面」。第二標準「保溫 ping 連發兩次無回應＝玩家離開就建議換幕」拍板，隨包 7 做。npm build＋check:i18n 綠。
- prompt-cache-optimization：包 7 保溫 ping 完成（拍板提前於包 6）——距上次推進 3.5 分鐘且視窗在前景時，替每條還活著的線讀一次既有快取續命（成本約為讓它過期重建的 1/12），問答隨即用 `truncate_from` 從 session 檔截掉、不留痕跡也不進劇情；連三次（約 12 分鐘）沒等到玩家推進就停手，改亮另一份「間隔太久、快取已清除」提醒（`sceneAwayHint`，10 語系）。截尾失敗才丟線（自我修復）；診斷加第九個標籤 `ping` 供額度分頁分項統計。假 CLI 建置抽成共用 helper。cargo test 214 綠、clippy 無新增警告、npm build（含 tsc）＋check:i18n＋npm test 全綠；前端計時器待真桌實測。
- prompt-cache-optimization：離開提醒加第二個條件——ping 三次之外還要該場紀錄超過 8000 字元才出聲（`SCENE_AWAY_HINT_MIN_CHARS`），紀錄還短時換幕只是白花一次摘要錢；保溫停在三次的省錢邏輯不變。npm build（含 tsc）綠。
- prompt-cache-optimization：包 6 額度分頁規格逐條拍板（本對話只討論不實作）——桌為主視圖（可切桌／取消選桌＝全桌總計，log 加桌欄位）；token＋命中率為主軸、金額只轉述 CLI 回報並標「參考牌價」（不自建價目表、不做 AI 查價）；grok 0.2.111 實測 end 事件有 total_cost_usd 待補讀；codex 僅回 token；agy 無任何用量回報（無頭輸出與 debug log 掃描皆零命中）拍板不整合；OpenRouter 之後補。煙霧實測花 grok 額度 $0.169。實作交下一對話、主線可用 Opus。未 commit。
- prompt-cache-optimization：包 6 額度分頁實作完成（使用者指定「先做完、驗收另開對話」）——設定頁新增「額度」分頁，以桌為主視圖（預設當前桌、可切桌、可看全桌總計），桌內按模型分行列輪數／輸入／讀到快取／命中率／輸出／花費，使用中的模型帶徽章，保溫 ping 自成一列，總計列含整體命中率；燈號＋原因句純規則判定（診斷九標籤＋原因九代碼，10 語系 44 鍵）。地基：log 加 world 欄（舊行歸未標桌）並一路帶進 API／CLI／lane 三條路，grok 補讀 total_cost_usd，agy 呼叫改寫一行 unreported（否則分頁上等於不存在）。金額只轉述 CLI 回報值、缺值標「≥」。新檔 usage_report.rs 純函式彙總＋2 條測試。cargo test 216 綠、clippy 無新增、npm build＋check:i18n 綠；**未開 app 看畫面、未跑真呼叫**，八項待驗清單寫進交接檔。9 語系翻譯外包 codex luna。
- prompt-cache-optimization：包 6 額度分頁實機驗收九項通過（數字與 jsonl 手算逐欄吻合、開桌生成認領成立、保溫 ping 實機 94.6% 且不進總計、十語系畫面），當場修掉六個顯示問題：下拉改列 app 現有每一桌、看總計時不出燈號句、燈號句不再重複兩次、俄文表頭撐寬視窗（縮欄名＋表格自捲）、「使用中」徽章折行、「單發」文案改成先說是哪種呼叫。另把「未標桌」概念拆成看得懂的「開桌生成／已刪掉的桌」，並讓一句話開桌的額度認領進生出來的那桌（`assign_pending_world`）；開發期沒帶桌別的 21 行舊 log 一次性改寫成實際那張桌。grok 金額與 agy 那一列因無額度／CLI 計量未定延後。cargo test 217 綠、clippy 無新增、npm build＋check:i18n＋npm test 全綠。**包 1–7 至此全部完成並驗收。**
- state-values-mvu：計畫全面改寫定稿（本對話只討論＋改計畫檔）——五張卡內嵌 JSON 逐張驗證，補上兩個缺口（第五張卡 orc-cave、勇者卡 30 條 EJS 腳本層）；目標升級為「機制格式」樹立自標準：欄位規則七型別資料驅動、觸發表（固定型 EJS 解析成純資料）、機制條目收編（勇者卡約 7.7 萬字元腳手架不再進 prompt）、統一協定聲明、未收編帳本；並拍板骰值本地擲、現值/上限對收 delta、拒收回饋自癒；AI 卡重構按鈕定位下期、本期交付格式規範文件當地基。分包 6→8。
- interface-card-panel：立案（本對話只拆卡＋立案不實作）——拆西幻卡證實介面殼在卡內 `extensions.regex_scripts`（44KB 殼＋24KB 開場，殼零 token，成本在世界書＋每回合狀態文字）；拍板做沙盒面板當相容選配，SCrypt DRM 卡不碰；「transcript 逐則加存原文」待帶給 state-values-mvu 包 4。加拆第二作者尋道太虛卡：按鈕代送只靠 `triggerSlash` 一個函式（沙盒墊片即可相容）、輸出契約同為 `<Status_block>`+YAML（與原生狀態欄同源）、卡自帶 promptOnly 剝舊狀態省 token（本地權威天然等效、可忽略）。再拆第三作者訓帝卡＝雲端載入器流派（殼在作者 GitHub Pages、回頭抓酒館內部資料，v1 不支援、偵測到退純文字）；其 VN 輸出協議（maintext＋6 選項＋資產詞彙表）收為將來原生 VN 皮的參考配方。未 commit。
- state-values-mvu：包 4（增量解析＋本地權威算術＋統一協定）完成——新模組 mechanism.rs 把 MVU 的 `<UpdateVariable>` JSON Patch 五種 op 解析成資料，再依欄位規則決定收不收：絕對值打數字欄一律拒收並在下一輪回合尾告知現值（自癒回饋，跟著逐則快照走）、現值/上限對的 delta 動現值而 replace 只認上限、骰值欄每回合本地重擲、上下限從卡片 `[mvu_update]` 規則表抽出來夾住、三類硬錯誤與夾限全部記帳落 `mechanism-log.jsonl`；機制腳手架條目（`[initvar]`／`[mvu_update]`／`format_message_variable` 巨集）匯入即停用不再送模型；統一協定聲明進 GM 凍結快照（只在增量桌）。順手帶 interface-card-panel 的交界：transcript 事件加存剝殼前原文 `raw`。勇者實卡煙霧：19 條規則、HP 500/500→420/500、Invasion 絕對值被拒留 1、Affection +500 夾到 200、Roll100 連擲 12 次 11 個不同值、壞逗號後的 patch 照樣套用。cargo test 262 綠、clippy／fmt 持平、tsc 綠；**未開 app 跑真桌**。實作外包兩隻 general-purpose subagent（model: opus），規格與驗收主線。
- state-values-mvu：包 5（注入策略＋分支切割）完成——回合尾從「整棵樹全送」改成只送相關分支：桌級＋玩家＋在場角色（在場名單取平欄 `present`，空著就不裁）；**手足規則**（主線煙霧測試後補）讓同容器裡沒有角色卡的分支照樣被裁，勇者卡 15 個英雄因此裁得動。換幕後第一輪送整棵樹對齊（`aligned_scene`，模型真的收到才記），角色線只拿自己那支且放機密段（chars 共用一條 session，放一般段會漏給下一個角色）。注入等級落地依 Fable 修正改寫：長文字欄不進回合尾也不進凍結快照，**變動落成 transcript 系統事件**（動態塊每輪重組、不落歷史，API 那條路模型下一輪就看不到）。變動標記 `HP：3920/4000（-80）` 跟著逐則快照走。面板每個分支加指認下拉（指認優先、同名自動比對備援，換綁自動解舊綁），`{{user}}` 只在顯示時代換，玩家那支預設展開。勇者實卡提示詞煙霧（未呼叫模型）：回合尾 3,127→530 字元、對齊輪 9,571、角色線自己那支 506 且沒漏別人。cargo test 272 綠、clippy 少一個警告、tsc＋build 綠；**未開 app 跑真桌**（要花使用者額度，沒代做）。實作外包兩隻 general-purpose subagent（model: opus，一後端一前端），規格、煙霧測試與手足規則主線。
- interface-card-panel：開工，包 1（Rust 讀取端）完成——介面殼的來源直接用匯入時就存下的原始卡檔，不新增任何儲存格式；`read_card_interfaces` 掃出每張卡「套在模型輸出上」的顯示腳本（停用／僅套 prompt／不作用在輸出的都濾掉），並偵測兩型不支援的卡：SCrypt 加密卡與雲端載入器卡（整段輸出換成一行從外部網址載入前端），偵測到只回報不支援、不解密也不繞過；壞卡一律跳過不報錯，面板絕不擋住對話。使用者拍板面板形式＝全螢幕覆蓋層、只渲染最新一則、開著時正文全交給殼自己畫。cargo test 272→277 綠。實作外包 general-purpose subagent（model: sonnet），規格與驗收主線。
- interface-card-panel：包 2（前端渲染）完成——卡片自帶的整頁介面現在真的畫得出來：把模型輸出套上卡片的顯示腳本、抽出內嵌的 HTML 殼，塞進只給 allow-scripts 的沙盒 iframe，聊天標題列一顆鈕整面蓋掉對話（沒帶介面的桌不會出現那顆鈕），Esc 或關閉鈕收；介面裡的按鈕點下去文字填進輸入框、絕不代按送出。沙盒碰不到 app 內部，所以在 iframe 內放一個誘餌輸入框把卡片戳宿主的動作攔下來轉成訊息；主線驗收時補了兩處子代理沒想到的實際問題（連點兩個行動會黏成一串、真卡有五種找輸入框的寫法）。西幻真卡端對端四項過（真開場訊息→角色選擇畫面、作者輸出範本→五大區塊都畫出來且 capture group 有換進去、沙盒文件帶橋接墊片且不再戳宿主、壞殼回退成沒介面），這條驗收不花模型額度。順手修掉上一包留下的 check:i18n 紅燈（ja playerLabel 是代稱不是按鈕文案，列入白名單）。vitest 14／tsc／check:i18n 十語系／build 全綠；**未在真 app 跑過**，留包 3。實作外包兩隻 general-purpose subagent（model: opus），規格與驗收主線。
- interface-card-panel：包 3（收邊）完成，v1 三包到齊——匯入角色卡時的提示改成依這張卡實際畫不畫得出介面分流（有介面就告訴玩家在哪開／加密卡與雲端載入器卡各自講清楚原因、其餘照常匯入／只有非顯示型腳本沿用舊句），十語系文案齊。並在真瀏覽器（vite dev + 沙盒 iframe）用西幻實卡跑完互動實測：五個分頁的介面完整畫出來，點「推薦行動1」宿主收到那句話、再點「推薦行動2」收到的就是第二句（連點不黏字的修正有效），開場角色選擇畫面選角後按「踏入命运」把整句身份設定送進輸入框（走的是另一套七種選擇器的程式碼，兩條路都通），console 無錯。tsc／check:i18n／build 全綠。剩使用者在真 app（WKWebView）匯入實卡驗收五項。十語系文案外包 general-purpose subagent（model: sonnet），其餘主線直寫。
- interface-card-panel：使用者第一輪實測回報「匯入西幻卡後標題列沒出現按鈕」，查出兩層原因並修完——表面是面板只畫最新一則模型輸出、空桌沒東西可畫（補上空桌退回卡片自帶開場白的路）；根因是**世界書卡的判定條件錯**：舊條件只看「人設少於 200 字」，西幻卡人設 988 字所以被判成角色卡，而匯成角色卡會把整包世界書條目丟掉（含卡片自訂的輸出格式規定），模型永遠不會吐出介面要的標籤塊。判定改看比重（條目≥3 且世界書字數≥人設×3，人設含 mes_example），真卡實測西幻 22 倍、尋道與訓帝人設為 0，三張都判對。使用者拍板：判成世界書卡就只給確認／取消，不再提供「照樣匯成角色卡」；世界書路徑會留下原始卡檔讓介面腳本與開場白跟著過去。另經使用者同意順帶修：匯成角色卡時也把卡片隨身的世界書條目帶進這桌（同名去重），判定失手也不會有人玩不動。真卡整條路驗過（38 條條目整包匯入、兩支介面腳本與開場白讀得到）。cargo test 282／vitest 14／tsc／check:i18n／build 全綠。Rust 外包 general-purpose subagent（model: opus）、十語系文案 sonnet，判定條件與根因分析主線。
- interface-card-panel：補上「帶介面的桌匯入完自動打開介面一次」（使用者拍板）——這類卡貼出來的開場在聊天裡只是孤零零一句「请选择你的身份」，玩家不點按鈕不會知道有整頁畫面；世界書路徑在問完開場白後開、角色卡路徑在匯入提示後開。順手把「該畫哪個殼」收成 interface-card.ts 的 findShell，面板與匯入流程共用同一套判斷（不會出現按鈕說有、打開卻空的）。vitest 14→17、tsc／check:i18n／build 全綠。主線直寫。
- interface-card-panel：介面裡按行動改成直接送出、畫面留在介面裡（使用者拍板）——原本填完字就關面板、要玩家自己回對話按送出，一回合把玩家踢出介面兩次，比 ST 差（ST 介面嵌在對話裡全程不離開）。現在點下去就送，右上角顯示 GM 正在打字，回覆到了就地重畫。卡片腳本自己送的照樣放行（ST 上是正常用法），但因為我們每收到新訊息就重載 iframe、「載入就送」會滾成無限迴圈燒額度，所以加煞車：玩家沒動作期間只准自動送一次，玩家一動就歸零，被擋下的改成填輸入框。墊片以捕獲階段記錄 isTrusted 時間戳判斷是不是玩家動的手；瀏覽器實測「腳本自己送」確實標成 gesture:false 兩次，但「玩家點的」那條因為瀏覽器窗格這輪不再把點擊送進沙盒 iframe（同一工具稍早是可以的）沒能再驗一次，留給使用者實機確認。vitest 17／tsc／check:i18n／build 全綠。主線直寫。
- interface-card-panel：使用者質疑「跟 ST 做法相同就不必擋任何東西」，查酒馆助手文件確認 ST 是每則訊息各自一個 iframe、有渲染深度設定、文件裡沒有任何防迴圈或節流——「載入就送」的卡在 ST 一樣會滾迴圈，作者不會那樣寫。故移除我先前加的自動送出煞車與墊片裡的 isTrusted 偵測，卡片要送就送；並補上雙緩衝（新殼先在背面那格 iframe 載好才翻面，中途舊畫面一直在不會閃白，兩格 key 固定成格號以免重新掛載白載一次）。卡片只按 /trigger 沒帶文字時＝直接要對象接話。vitest 17／tsc／check:i18n／build 全綠；雙緩衝的視覺效果要在真 app 看。主線直寫。
- interface-card-panel：使用者第二輪實測回報「點了只發到介面、不會發給 AI」，查出兩個問題並修——(1) 純世界書桌一個角色都沒有，enterTable 是「挑第一個未封存角色」當發言對象，於是對象是空的、送出去沒人接（輸入框也是鎖的），改成沒有角色時預設 GM；(2) 送出後最新一則是玩家發言、算不出殼，覆蓋層因此直接關掉、看不到 GM 正在打字，改成只要「現在有殼可顯示」就留著等回覆，切桌時清掉雙緩衝兩格。vitest 17／tsc／check:i18n／build 全綠。主線直寫。
- interface-card-panel：使用者第三輪實測回報「GM 回話了但介面沒變」，查出最關鍵的根因——卡片的『回复规则』條目要求「每次回覆只能輸出一個 XML 資料塊、禁止其他內容」，而我們在回合尾（最後說話的位置）要求「插入一段旁白＋state 圍欄」，模型聽我們的，吐出的散文對不上卡片 regex，介面永遠停在開場。修法：這桌有可渲染的卡片介面時，導演指示整條換成「完全依照卡片規定的輸出格式」，並自動點名世界書裡教這個格式的條目（從顯示腳本的 find_regex 抽字面標籤回頭比對世界書內容）；沒有卡片介面的桌逐字不變。拿使用者真實那桌的資料驗過：2 支腳本、38 條世界書、正確點名「回复规则」、指令裡沒有 state 圍欄與下一位點名。cargo test 285／vitest 17／tsc／build 全綠；模型會不會照做要真的呼叫一次才知道（花使用者額度，沒代做）。副作用待拍板：模型改吐 XML 後聊天畫面會看到整段 XML。實作外包 general-purpose subagent（model: opus），根因分析與規格主線。
- interface-card-panel：本輪收工。v1 三包＋使用者三輪實機實測的修正全部完成並 commit（世界書卡判定與路徑、純世界書桌預設 GM 對象、送出後留在介面、GM 改照卡片格式輸出、雙緩衝、匯入即開介面）。cargo test 285／vitest 17／tsc／check:i18n／build 全綠。下次開工第一件事＝實測 GM 是否照卡片格式輸出、介面會不會就地換新畫面。使用者實測時提出兩項延後處理：模型改吐 XML 後聊天會看到整段 XML（要不要收成一行提示）、以及這類卡每回合重吐整份狀態很燒額度（能否沿用 state-values-mvu 的本地權威只吐變動）。
- interface-card-panel：**v1 實機驗收全數通過**（使用者實測：匯入→開介面→點行動→直接送出→GM 照卡片格式回覆→介面就地換成新畫面）。兩項待拍板結案：聊天區把 XML 收成一行提示＝不做（玩家正常不關介面，且顯示層收合省不到額度）；省額度改列 v2 首要項目，並釐清真正的成本在歷史——每輪整包 XML 原封留在 transcript，下一輪連同前面每一輪一起送模型，玩十輪等於重送十份狀態表；坑在於劇情正文就在資料塊裡，要留正文、砍掉重複的地圖矩陣／委託清單／角色表，屬結構性裁剪需另開一包。
