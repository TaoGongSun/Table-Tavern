# 2026-07-23

- 00:20 +0800 — `completed` — `ui-i18n-switch`：UI 語系切換（zh-TW／en）——新增 src/i18n.ts 字典、App.tsx 全字串走 t()、側欄語言下拉即切即存；npm build rc=0、cargo test 31 passed，commit fcd7e35
- 00:20 +0800 — `completed` — `post-mvp-i18n-language-rule`：LANGUAGE_RULE 改 language_rule(lang) 依 config.preferences.language 注入（zh 繁中規範／en 英文規範），隨 ui-i18n-switch 一併結案，commit fcd7e35
- 00:20 +0800 — `created` — `cli-auto-connect`、`ui-settings-panel`：使用者新增兩項需求入列待辦（CLI 背景自動接＋登入跳轉自動回；設定按鈕調文字大小等）
- 00:35 +0800 — `note` — `ui-i18n-switch` 補遺：視窗標題「桌面酒館」改固定顯示「Table Tavern」（tauri.conf.json，使用者拍板不跟語系連動），commit 2363b49
- 00:40 +0800 — `created` — `sample-world-i18n`：範例桌內容依語系產生入列待辦（en 使用者首開拿英文範例桌）
- 01:00 +0800 — `note` — TASKS.md Todo 改依優先序排列（由上往下挑），st-import 移除過時的「等 MVP 驗收」字樣
- 01:10 +0800 — `created` — `transcript-export`：一鍵下載跑團紀錄入列待辦（排第 3 位，JSONL 已在背景落地，補匯出 Markdown＋存檔鈕）
- 10:25 +0800 — `created` — `ui-layout-rework`：版面重構入列待辦（NewPlan 新增 §9.4 拍板：角色卡移左側欄＋桌列表可摺疊＋單一設定入口內分頁）；`ui-settings-panel` 改為「單一設定視窗內分頁（外觀預設／AI 連線）」並等 layout 定版面；`post-mvp-st-import` 增補存 PNG＋角色圖顯示/隱藏開關（§5.2）。純文件變更，額度考量暫不動工
- 15:10 +0800 — `progress` — `ui-layout-rework`：版面重構程式碼完成（Codex gpt-5.6-terra 實作、主線驗收）：側欄改 details 摺疊桌區（localStorage `table_list_open`）＋直排角色卡（頭像獨立格預留圖片位）＋建卡表單搬入，cast-row 移除、死 CSS 清零；npm build 綠、既有功能逐項行號核對。剩使用者真實 App 視覺驗收即結案
- 15:50 +0800 — `progress` — `post-mvp-st-import`：後端切片完成（Codex gpt-5.6-terra 實作、主線拍板欄位對應與驗收）：新 import.rs（PNG tEXt chara／純 JSON、V2+V1、手寫 base64 無新依賴）＋import_character command＋原始檔保留（<name>.png／.import.json）＋同名不覆蓋；cargo test 36 綠。剩前端匯入鈕＋角色圖開關，等 ui-layout-rework 視覺驗收後接
- 16:25 +0800 — `progress` — `transcript-export`：前後端完成（Codex gpt-5.6-terra 實作、主線驗收）：data.rs export_transcript_markdown（場景數值排序＋zh/en 雙語格式＋測試）＋lib.rs command 寫下載資料夾＋App.tsx header 匯出鈕＋revealItemInDir；cargo test 38 綠、npm build 綠。剩使用者實測按鈕出檔。另記：codex companion 外層 job 狀態卡 running 的假象，之後改以原生 session log（~/.codex/sessions）確認 final answer
- 21:35 +0800 — `progress` — `post-mvp-st-import`：前端切片完成（主線 Fable 5 直做，未外包）：show_image 旗標進卡 frontmatter（缺鍵預設 true、舊卡免遷移）＋read_character_image command（base64）＋側欄匯入卡鈕（PNG/JSON 讀 bytes、顏色輪選）＋角色圖顯示與 CardEditor 開關＋i18n 三鍵＋CSS；cargo test 40 綠、npm build 綠；驗收用範例卡（夜鶯.card.png）已交使用者。剩使用者實測即結案
- 22:15 +0800 — `done` — `post-mvp-st-import`：使用者實測通過（匯入夜鶯範例卡→側欄顯圖→開關切回 emoji，附截圖），結案
- 22:15 +0800 — `progress` — `transcript-export`：依使用者回饋改存檔位置自選（主線 Fable 5 直做）：lib.rs export_transcript 改收 path、App.tsx 用 plugin-dialog save() 跳另存對話框（預設檔名桌名＋時間戳、取消即中止）、新依賴 tauri-plugin-dialog（原生對話框必要）；cargo test 40 綠、0 warning、npm build 綠。剩使用者實測存檔流程
- 23:05 +0800 — `done` — `transcript-export`：使用者實測另存對話框存檔通過（附截圖），結案
- 23:05 +0800 — `done` — `ui-layout-rework`：使用者視覺驗收通過（截圖示角色卡左欄＋桌列表摺疊正常），結案
- 23:10 +0800 — `progress` — `ui-settings-panel`：程式碼完成（主線 Fable 5 直做）：SettingsWindow modal 兩分頁（外觀預設頁：語言＋文字大小即改即存 config.preferences.text_size 套根字級；AI 連線頁原表單搬入）、側欄底改單一設定鈕、i18n 增刪鍵、root line-height 改 1.5；npm build 綠。剩使用者視覺驗收
- 23:30 +0800 — `progress` — `ui-settings-panel`：使用者初驗通過（外觀頁預設、AI 頁設定保留），回饋文字大小要更小檔位→改五檔 xs/s/m/l/xl＝10/12/14/16/18px（預設 l 維持原視覺），i18n 五鍵；npm build 綠。剩使用者複驗五檔
