# Activity for 2026-08-13

<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "CLI 生命週期驗證完成，臨時任務結案", "task_id": "zz-cli-verify", "timestamp": "2026-08-13T01:28:30.817128+08:00"} -->
- 01:28 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/zz-cli-verify`：CLI 生命週期驗證完成，臨時任務結案
<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "App.tsx 6890→1323 行，App() 2908→1195 行／59→18 state／13→2 ref／105→47 區塊。三批 15 切片全數完成：第一批元件搬移（CardEditor、WorldEditor、SettingsWindow／SettingsForm／UsageTab、atoms）、第二批五個 controller hook＋GenerateTableDialog、第三批四支受控 view（TableSidebar 405／WorkspaceHeader+StateBar 302／PlayView 305／MainView 179）。過程不改行為。\n\n證據：每切片都跑 vitest 126＋tsc＋build＋check:i18n（99 顆按鈕、10 語系，全程只允許持平或上升，最終與切片 14 後逐字相同）；每切片都做搬移前後逐行 diff，殘差只有 prettier 折行——切片 13 的五段逐行完全相同，切片 15 去縮排後只剩一處三元式折行。切片 14 唯一的等價改寫（捲到底 effect 由 [mainView] 改成掛載時跑一次）在 commit message 內說明並經實機驗證。\n\n第三批完成後跑完整 16 項回歸（2026-08-13，打當前碼的 release 包、自建測試桌、computer-use 驅動）：機械項 1、2、3、4、8、9、10 開關、11、13 前半、14、15、16 全數通過；7 的機械面（換幕鈕依 hasEvents 開關、canUndoScene 兩顆補救鈕進出）順帶通過。測試桌已刪、使用者 26 桌今天零改動、last_world／theme／text_size 全復原。\n\n留給使用者（會花額度）：5 的翻譯語感、6 聊天一輪、7 換幕／分岔／退回／重生摘要、10 的殼內送出、12 的 AI 生成。\n仍未驗的子項：9 的計數器、13 後半與 14 的「無桌時補範例桌」、15 的裁圖與未儲存離開 guard、11 的贊助主題鎖。\n三個既有問題（非拆分造成，已各自立案）：act-fork-missing、leave-guard-switch-table、卡片介面覆蓋層 Esc 關不掉。\n\n與規格預估 440–570 行的差距：實際 1323 行。App() 剩的 1195 行裡 474 行是仍在 root 的 JSX（十支子元件的呼叫本身），其餘是規格明列必須留在 composition root 的桌次生命週期 9 支 handler、卡片畫面切換 handler 與 controller 組裝。再往下壓只能把 props 打包成物件或引入 context，會犧牲受控元件可讀性，依指示不為壓行數硬拆。", "task_id": "app-split", "timestamp": "2026-08-13T16:20:57.690144+08:00"} -->
- 16:20 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/app-split`：App.tsx 6890→1323 行，App() 2908→1195 行／59→18 state／13→2 ref／105→47 區塊。三批 15 切片全數完成：第一批元件搬移（CardEditor、WorldEditor、SettingsWindow／SettingsForm／UsageTab、atoms）、第二批五個 controller hook＋GenerateTableDialog、第三批四支受控 view（TableSidebar 405／WorkspaceHeader+StateBar 302／PlayView 305／MainView 179）。過程不改行為。

證據：每切片都跑 vitest 126＋tsc＋build＋check:i18n（99 顆按鈕、10 語系，全程只允許持平或上升，最終與切片 14 後逐字相同）；每切片都做搬移前後逐行 diff，殘差只有 prettier 折行——切片 13 的五段逐行完全相同，切片 15 去縮排後只剩一處三元式折行。切片 14 唯一的等價改寫（捲到底 effect 由 [mainView] 改成掛載時跑一次）在 commit message 內說明並經實機驗證。

第三批完成後跑完整 16 項回歸（2026-08-13，打當前碼的 release 包、自建測試桌、computer-use 驅動）：機械項 1、2、3、4、8、9、10 開關、11、13 前半、14、15、16 全數通過；7 的機械面（換幕鈕依 hasEvents 開關、canUndoScene 兩顆補救鈕進出）順帶通過。測試桌已刪、使用者 26 桌今天零改動、last_world／theme／text_size 全復原。

留給使用者（會花額度）：5 的翻譯語感、6 聊天一輪、7 換幕／分岔／退回／重生摘要、10 的殼內送出、12 的 AI 生成。
仍未驗的子項：9 的計數器、13 後半與 14 的「無桌時補範例桌」、15 的裁圖與未儲存離開 guard、11 的贊助主題鎖。
三個既有問題（非拆分造成，已各自立案）：act-fork-missing、leave-guard-switch-table、卡片介面覆蓋層 Esc 關不掉。

與規格預估 440–570 行的差距：實際 1323 行。App() 剩的 1195 行裡 474 行是仍在 root 的 JSX（十支子元件的呼叫本身），其餘是規格明列必須留在 composition root 的桌次生命週期 9 支 handler、卡片畫面切換 handler 與 controller 組裝。再往下壓只能把 props 打包成物件或引入 context，會犧牲受控元件可讀性，依指示不為壓行數硬拆。
<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "App.tsx 拆分完成：6890→1082 行，App() 2908→1024 行／59→18 state／13→2 ref／105→47 區塊／JSX 425→257 行。共 16 個切片、16 個 commit（前置 66a91ed → 切片 16 19f0d33），每片都以「搬移前後逐行 diff」對照行為，全程不改行為。\n\n產出：views/ 11 支元件（CardEditor 780、WorldEditor 1351、SettingsWindow 265＋SettingsForm 609、UsageTab 271、atoms、GenerateTableDialog 298、TableSidebar 405、WorkspaceHeader 302、PlayView 305、MainView 179、Onboarding 70、ImportDialogs 231、CardInterfaceOverlay 56）＋controllers/ 5 支（cardInterface 233、tableState 187、characters 363、chat 464、imports 457）＋外抽的 drag-reorder.ts／backend-contracts.ts／model-catalog-store.ts。五條硬約束全數守住（controller public API 穩定、依賴誠實、latest-ref 只用於 event callback、enterTable 同步提交區零 await、不做全域 api.ts）。\n\n驗證：vitest 126／npx tsc --noEmit／npm run build／check:i18n 99 顆按鈕（10 語系）全綠；切片 16 的 i18n 輸出與切片 15 後逐字相同。第三批完成後跑過完整 16 項回歸的 12 項（1／2／3／4／8／9／10 開關／11／13 前半／14／15／16 全通過）；切片 16 另跑針對回歸全通過——Onboarding 顯示條件、匯入身分框（取消＋匯入成角色卡 38 條）、路由框三顆（取消／匯進這桌 100 條／開新桌並匯入）、開場白面板（展開／收合／先不要／遮罩關閉）、卡片介面（自動開／header 鈕開／✕ 關／換桌換殼重掛）、A→B→A 換桌無殘留。回歸全在自建測試桌上做，做完刪除；使用者 26 桌零改動，last_world／theme／text_size 復原。\n\n留給使用者自驗（會花 AI 額度，本任務一律不代按）：5 開場白翻譯語感、6 聊天一輪、7 換幕／分岔／退回／重生摘要、10 卡片介面殼內送出、12 AI 生成新桌與世界書重構。\n仍未驗的子項：9 的計數器、13 後半與 14 的「無桌時補範例桌」、15 的裁圖與未儲存離開 guard、11 的贊助主題鎖。\n三個既有問題（非拆分造成，已各自立案）：ActReader「從這一幕繼續」不渲染（act-fork-missing）、換桌不攔未儲存的角色卡（leave-guard-switch-table）、卡片介面覆蓋層 Esc 關不掉（焦點在沙盒 iframe，宿主收不到 keydown）。\n\n與規格原估 440–570 行的差距：終值 App() 1024 行＝257 行 JSX＋controller 組裝與跨域 handler。原估低估了跨域協調的份量；剩下的桌次生命週期、換幕／匯出、復原匯入、卡片導航與顯式 props 就是真正的 composition root，再往下只能打包 props、引 context 或做高度耦合的跨 controller hook，經 Sol 與 Claude 判定均不做。", "task_id": "app-split", "timestamp": "2026-08-13T17:10:59.473655+08:00"} -->
- 17:10 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/app-split`：App.tsx 拆分完成：6890→1082 行，App() 2908→1024 行／59→18 state／13→2 ref／105→47 區塊／JSX 425→257 行。共 16 個切片、16 個 commit（前置 66a91ed → 切片 16 19f0d33），每片都以「搬移前後逐行 diff」對照行為，全程不改行為。

產出：views/ 11 支元件（CardEditor 780、WorldEditor 1351、SettingsWindow 265＋SettingsForm 609、UsageTab 271、atoms、GenerateTableDialog 298、TableSidebar 405、WorkspaceHeader 302、PlayView 305、MainView 179、Onboarding 70、ImportDialogs 231、CardInterfaceOverlay 56）＋controllers/ 5 支（cardInterface 233、tableState 187、characters 363、chat 464、imports 457）＋外抽的 drag-reorder.ts／backend-contracts.ts／model-catalog-store.ts。五條硬約束全數守住（controller public API 穩定、依賴誠實、latest-ref 只用於 event callback、enterTable 同步提交區零 await、不做全域 api.ts）。

驗證：vitest 126／npx tsc --noEmit／npm run build／check:i18n 99 顆按鈕（10 語系）全綠；切片 16 的 i18n 輸出與切片 15 後逐字相同。第三批完成後跑過完整 16 項回歸的 12 項（1／2／3／4／8／9／10 開關／11／13 前半／14／15／16 全通過）；切片 16 另跑針對回歸全通過——Onboarding 顯示條件、匯入身分框（取消＋匯入成角色卡 38 條）、路由框三顆（取消／匯進這桌 100 條／開新桌並匯入）、開場白面板（展開／收合／先不要／遮罩關閉）、卡片介面（自動開／header 鈕開／✕ 關／換桌換殼重掛）、A→B→A 換桌無殘留。回歸全在自建測試桌上做，做完刪除；使用者 26 桌零改動，last_world／theme／text_size 復原。

留給使用者自驗（會花 AI 額度，本任務一律不代按）：5 開場白翻譯語感、6 聊天一輪、7 換幕／分岔／退回／重生摘要、10 卡片介面殼內送出、12 AI 生成新桌與世界書重構。
仍未驗的子項：9 的計數器、13 後半與 14 的「無桌時補範例桌」、15 的裁圖與未儲存離開 guard、11 的贊助主題鎖。
三個既有問題（非拆分造成，已各自立案）：ActReader「從這一幕繼續」不渲染（act-fork-missing）、換桌不攔未儲存的角色卡（leave-guard-switch-table）、卡片介面覆蓋層 Esc 關不掉（焦點在沙盒 iframe，宿主收不到 keydown）。

與規格原估 440–570 行的差距：終值 App() 1024 行＝257 行 JSX＋controller 組裝與跨域 handler。原估低估了跨域協調的份量；剩下的桌次生命週期、換幕／匯出、復原匯入、卡片導航與顯式 props 就是真正的 composition root，再往下只能打包 props、引 context 或做高度耦合的跨 controller hook，經 Sol 與 Claude 判定均不做。
<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "不再需要處理：2026-08-13 使用者實機截圖確認單幕閱讀（第 2 幕）標題列右側已出現「從這一幕繼續」按鈕，與「匯出本幕」「返回」並列。app-split 15 切片跑完後自行恢復，未做任何針對此問題的修改，成因不明。若日後復發再重新立案。", "task_id": "act-fork-missing", "timestamp": "2026-08-13T17:16:54.286253+08:00"} -->
- 17:16 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/act-fork-missing`：不再需要處理：2026-08-13 使用者實機截圖確認單幕閱讀（第 2 幕）標題列右側已出現「從這一幕繼續」按鈕，與「匯出本幕」「返回」並列。app-split 15 切片跑完後自行恢復，未做任何針對此問題的修改，成因不明。若日後復發再重新立案。
<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "換桌／換幕／跳單幕閱讀全數接上未儲存守門。src/App.tsx +31 −6：7 處入口加 await canLeaveEditor()，都放在任何檔案副作用之前——switchTable、newTable、openGenerateTable（新增，守門放在開對話框那一刻而非生成完成後，否則等 AI 生完才問、取消就白花一次生成又留下進不去的桌）、openTableForImport（走新增的 canLeaveRef，useCallback 閉包抓不到最新主欄畫面）、advanceScene（WorkspaceHeader 渲染不受 mainView 條件包裹，編輯畫面下換幕鈕照樣按得到）、openSceneReader（新增，前幕浮層點一幕）、answerRegen 的範例桌重生。守門不收進 enterTable：那時 create_world 之類副作用已發生，取消會留下半張桌並誤跑 reclaimIfUntouched。deleteTable 照使用者拍板不追問（整桌都要刪，刪桌確認框已足夠）。兩支編輯器與 leaveGuard 機制未動——未儲存本來就是同一套（CardEditor／WorldEditor 各算 unsavedCount、共用 confirmLeave 與 leaveGuard ref、共用文案，世界書條目已納入：新條目算未儲存、既有條目改到一半自動存）。\\n\\n自驗：npx tsc --noEmit 0、vitest 126、npm run build、check:i18n 99 顆按鈕 10 語系全綠。\\n\\n實機驗收（2026-08-13 18:00，npm run tauri build 打的 release 包、自建測試桌、computer-use 驅動）八條全過，每條都驗「取消＝留在原地且改動還在」與「確定＝照常執行且不再追問」：編輯中換桌、開新的一桌、一句話開桌（問框先出、生成對話框沒開）、換幕（問框先出、沒花額度）、前幕點一幕（在 Dark Wolf 三幕桌上測，取消留在世界設定、確定進單幕閱讀）、匯入卡走「開新桌並匯入」（取消後桌列表沒多出半張桌）、世界設定改 world.md 後換桌、刪桌（世界設定有未儲存時只跳「永久刪除這桌」、不追問未儲存）。測試桌測完刪除；26 桌零改動、last_world 回到 Dark Wolf。\\n\\n未實測的一條：answerRegen 的範例桌重生。config.json 的 sample_regen_asked 已是 true，設定頁改語言不再問「要不要重生範例桌」，這條路在這台機器上不會被觸發；程式碼與 switchTable 同一行守門、同一位置（所有 invoke 之前），以審查覆蓋。\\n\\n過程記錄：第一輪實測用 npm run tauri dev 起開發版，但使用者原本已開著 16:45 打的 release 版，兩個視窗完全重疊、點到的是舊碼那個，誤判成守門失效。教訓＝GUI 實測前先 ps 確認沒有既有實例，並優先用剛打的 release 包而非 dev 版。", "task_id": "leave-guard-switch-table", "timestamp": "2026-08-13T18:05:40.402551+08:00"} -->
- 18:05 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/leave-guard-switch-table`：換桌／換幕／跳單幕閱讀全數接上未儲存守門。src/App.tsx +31 −6：7 處入口加 await canLeaveEditor()，都放在任何檔案副作用之前——switchTable、newTable、openGenerateTable（新增，守門放在開對話框那一刻而非生成完成後，否則等 AI 生完才問、取消就白花一次生成又留下進不去的桌）、openTableForImport（走新增的 canLeaveRef，useCallback 閉包抓不到最新主欄畫面）、advanceScene（WorkspaceHeader 渲染不受 mainView 條件包裹，編輯畫面下換幕鈕照樣按得到）、openSceneReader（新增，前幕浮層點一幕）、answerRegen 的範例桌重生。守門不收進 enterTable：那時 create_world 之類副作用已發生，取消會留下半張桌並誤跑 reclaimIfUntouched。deleteTable 照使用者拍板不追問（整桌都要刪，刪桌確認框已足夠）。兩支編輯器與 leaveGuard 機制未動——未儲存本來就是同一套（CardEditor／WorldEditor 各算 unsavedCount、共用 confirmLeave 與 leaveGuard ref、共用文案，世界書條目已納入：新條目算未儲存、既有條目改到一半自動存）。\n\n自驗：npx tsc --noEmit 0、vitest 126、npm run build、check:i18n 99 顆按鈕 10 語系全綠。\n\n實機驗收（2026-08-13 18:00，npm run tauri build 打的 release 包、自建測試桌、computer-use 驅動）八條全過，每條都驗「取消＝留在原地且改動還在」與「確定＝照常執行且不再追問」：編輯中換桌、開新的一桌、一句話開桌（問框先出、生成對話框沒開）、換幕（問框先出、沒花額度）、前幕點一幕（在 Dark Wolf 三幕桌上測，取消留在世界設定、確定進單幕閱讀）、匯入卡走「開新桌並匯入」（取消後桌列表沒多出半張桌）、世界設定改 world.md 後換桌、刪桌（世界設定有未儲存時只跳「永久刪除這桌」、不追問未儲存）。測試桌測完刪除；26 桌零改動、last_world 回到 Dark Wolf。\n\n未實測的一條：answerRegen 的範例桌重生。config.json 的 sample_regen_asked 已是 true，設定頁改語言不再問「要不要重生範例桌」，這條路在這台機器上不會被觸發；程式碼與 switchTable 同一行守門、同一位置（所有 invoke 之前），以審查覆蓋。\n\n過程記錄：第一輪實測用 npm run tauri dev 起開發版，但使用者原本已開著 16:45 打的 release 版，兩個視窗完全重疊、點到的是舊碼那個，誤判成守門失效。教訓＝GUI 實測前先 ps 確認沒有既有實例，並優先用剛打的 release 包而非 dev 版。
