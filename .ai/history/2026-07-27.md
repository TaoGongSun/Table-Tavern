# 2026-07-27

- character-image-avatar：app 內角色圖管理（加入／更換／移除全身圖，react-easy-crop 2:3 裁切）＋圓形頭像（256 正方形存檔、CSS 圓形黑框）取代 emoji，後端五指令＋4 測試，cargo 80 綠＋npm build 綠＋tauri dev 冒煙過，等使用者實測。commit fa4d855
- character-image-avatar 回饋修訂：✎ 鉛筆鈕改「編輯」字鈕（固定 12px 不隨字級縮、加底色），npm build 綠。
- character-image-avatar 回饋修訂：檔位寶石「中」從卡右上角搬到名牌後面同一列，不再與編輯鈕互遮，npm build 綠。
- character-image-avatar 回饋修訂：編輯頁角色圖點擊開大圖 lightbox（92vw/vh、zoom-in 手勢提示、點任意處關閉），npm build 綠。
- character-image-avatar 回饋修訂：app 圖示白底修正——icon-source.svg 直接經 tauri icon 重生全套（icns/ico/png/Square 磚），角落像素驗證 (0,0,0,0) 全透明；先前誤解成卡片圖窗的改動已 revert。commit 見本行
- character-image-avatar 結案：使用者實測全數通過（角色圖＋頭像＋lightbox＋編輯字鈕＋寶石移位＋透明圖示），Mac DMG 0.2.0 打包驗證過。新增 sponsor-features 待辦（作者頁＋5 主題＋AI 生圖，未拍板不動工）。
- Win 版打包（使用者指示）：run 30246665645 success（commit d1caa4a，含角色圖三件組與透明圖示），artifact table-tavern-windows-unsigned 7.41MB，等真 Windows 機驗收（含圖示去背確認）。
- sponsor-features：規格拍板寫入任務檔（贊助包解鎖、生圖免費 3 次含提示詞、主題 ☕ 標記＋關窗復原試看、推銷不主動彈窗），優先序待拍板。
- sponsor-features：優先序拍板（作者頁→配色→AI 生圖），作者頁實作完成（設定第三分頁：頭像＋文案＋Ko-fi 占位鈕），npm build 綠。
- sponsor-features：配色 +5 實作完成（codex terra 外包＋主線複驗），五套主題＋色票試看機制；登記 Ko-fi 導購歧義議程。
- sponsor-features：AI 生圖後端完成（Images API＋CLI 掃圖＋來源參數，cargo 85 綠）；四家 CLI 生圖實測結論寫入交接。
- sponsor-features：AI 生圖前端完成（生成對話框＋來源下拉＋免費3次＋介紹頁），三項全數實作完畢等實測。
- sponsor-features：修 CLI 生圖權限（聊天鎖工具不變；生圖呼叫 codex workspace-write／agy skip-permissions／grok always-approve），cargo 85 綠。
- sponsor-features：生成歷史圖庫完成（原圖落地＋縮圖列重裁/撿回＋X 刪除＋分批載入），cargo 91 綠＋build 綠。
- sponsor-features：收工交接（圖庫待實測；登記「常用提示詞標籤」議程含分析結論：v1 建議內建精選清單）。
- character-card-avatar-issues：新增待辦，登記使用者實測後的五項回饋（頭像與文字間距、角色卡改名、圖像操作未計入未儲存變更、移除頭像缺警告、建卡空名炸 `invalid name: ""`），五項皆已定位到檔案:行號寫進任務檔與交接檔，未動程式碼。
- character-card-avatar-issues：拍板第 3 項改「圖像操作暫存、按儲存才落地並計入未儲存計數」、第 2 項改名要回填歷史引用；第 1 項間距修完（`.avatar-round` margin 2px 補回 box-shadow 黑框、角色卡圓頭像時取消名牌 wedge 咬合、晶片 gap 0.5em、編輯頁大頭像下緣 0.6rem），npm build 綠＋Playwright before/after 截圖對照確認。
- cli-detect-state：新增任務並實作完成。設定頁重開時 CLI 列全灰＋顯示「一鍵安裝」的根因是 `clis` 初值空陣列（尚未偵測＝未偵測到），空窗期可誤按安裝；三件一起修——前端三態「正在偵測…」不給按鈕、後端 `tokio::join!` 四支並行＋各 5 秒 timeout＋kill_on_drop、偵測結果模組級快取跨畫面共用。cargo 93 綠、npm build 綠、並行實測 2.8 倍（55ms vs 153ms）。commit 28e7234
- 23:35 +0800 — `stable-id-storage` — 登記提案任務：桌與角色改用代碼定址、顯示名改欄位（使用者提案）。任務檔含四個待拍板題（劇情紀錄發言者存代碼還是含名字快照／範圍／代碼格式／舊資料處理）與影響範圍估算；主線原本以「中央對照表會壞」反對，已撤回（名字放各自資料夾內即可，不需中央表）。交接檔標註：若採用則「生成圖庫目錄放錯層」不用先修。
- character-card-avatar-issues：GM 卡書皮方案完成並經使用者七主題驗收（深皮革＋燙金內框＋各主題 `--gm-leather` 連動皮革色；高度拍板維持與有圖角色卡等高 69px；Codex 生圖任務取消，書本小圖沿用）。commit 7597061。另立新規約：交接檔已完成項目搬 `.ai/handoffs/archive/<task-id>-completed.md`、交接檔只留連結（已寫進 tavern-handoff SKILL.md），本任務交接檔已照辦（43→19 行）。
