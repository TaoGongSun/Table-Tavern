# Activity for 2026-09-03

<!-- event {"kind": "completed", "project_id": "local-60148748387b4307aae667bb71a54f6c", "summary": "transport.rs 5472 行拆進 transport/ 八個領域檔（messages／state_view／context／assemble／turns／arrivals／response／client）＋facade mod.rs＋test_support.rs，commit 8f26fb7。機械驗收：production 頂層 item 76→76、body 逐 byte 相同、僅 7 個必要 pub(super)；測試函式 539 支拆前拆後同數、transport 91 支名稱一一對應、macOS cargo test 535 passed／0 failed；RUSTFLAGS=-Dprivate_interfaces cargo check --all-targets 零警告零錯誤。facade 原本照抄 52 個對外項目，其中 8 個（PERSON_ARRIVAL_PREFIX、DEFAULT_IMAGE_MODEL、StreamOutcome、extract_delta、extract_usage、active_worldbook_entries、character_state_block、LaneTurn）在 transport/ 外零呼叫端，re-export 只換來 5 條 unused import 警告，經拍板刪除、驗收條文同步改為「保住有呼叫端的 44 條路徑」；8 者本體仍 pub 且都有 transport/ 內呼叫端，無死程式。實機 smoke test 走 CLI（claude）在既有桌完成：玩家送出訊息→GM 串流回覆落檔（cached 5877／prompt 7466）、角色切換三處同步、角色發言走 chars 線落 dialogue、state.json 的 time 隨回合更新。視覺狀態欄未驗（本機無任何桌滿足顯示條件），API 路徑未跑（使用者拍板只走 CLI）。期間另發現角色線 drop-lane／rewrite-failed，成因在未被本案動到的 lanes.rs，另立 chars-lane-rewrite-drop。", "task_id": "transport-split", "timestamp": "2026-09-03T18:47:27.087316+08:00"} -->
- 18:47 +0800 — `completed` — `local-60148748387b4307aae667bb71a54f6c/transport-split`：transport.rs 5472 行拆進 transport/ 八個領域檔（messages／state_view／context／assemble／turns／arrivals／response／client）＋facade mod.rs＋test_support.rs，commit 8f26fb7。機械驗收：production 頂層 item 76→76、body 逐 byte 相同、僅 7 個必要 pub(super)；測試函式 539 支拆前拆後同數、transport 91 支名稱一一對應、macOS cargo test 535 passed／0 failed；RUSTFLAGS=-Dprivate_interfaces cargo check --all-targets 零警告零錯誤。facade 原本照抄 52 個對外項目，其中 8 個（PERSON_ARRIVAL_PREFIX、DEFAULT_IMAGE_MODEL、StreamOutcome、extract_delta、extract_usage、active_worldbook_entries、character_state_block、LaneTurn）在 transport/ 外零呼叫端，re-export 只換來 5 條 unused import 警告，經拍板刪除、驗收條文同步改為「保住有呼叫端的 44 條路徑」；8 者本體仍 pub 且都有 transport/ 內呼叫端，無死程式。實機 smoke test 走 CLI（claude）在既有桌完成：玩家送出訊息→GM 串流回覆落檔（cached 5877／prompt 7466）、角色切換三處同步、角色發言走 chars 線落 dialogue、state.json 的 time 隨回合更新。視覺狀態欄未驗（本機無任何桌滿足顯示條件），API 路徑未跑（使用者拍板只走 CLI）。期間另發現角色線 drop-lane／rewrite-failed，成因在未被本案動到的 lanes.rs，另立 chars-lane-rewrite-drop。
