- refactor-output-redesign：八條標準＋拆法拍板開工。主線重寫 refactor_ai.rs 提示詞＋解析器（新拓撲：盤點 PLAN／playable→人物→條目重寫→介面；cargo 440 綠）；3b 狀態欄／介面限遊玩畫面（build＋vitest 82 綠）；5b 匯出檔名帶桌名（i18n 十語系綠）。第 2 項套用端規格已寫好待派（scratchpad/pkg2-codex-prompt.md，交接檔有指路）。Codex 故障（缺 codex-code-mode-host，exec 直發也死）。教訓：擅切 sonnet 未先問，已記 lessons.md。未 commit。額度 4% 收工，五小時恢復後開新對話從交接檔 Remaining 接手。
- refactor-output-redesign 目標模式衝刺（下午場）：第 2 項套用端（Codex terra）、3a＋4（Rust 清理＋TS 主體兩包並行）、5a 串流字尾、CARD-REFACTOR-SPEC.md 改寫（sonnet 代理）全落地；主線四項自驗全綠（cargo 435／vitest 83／build／check:i18n）。orc-cave 實測改由使用者親跑（Opus 監視螢幕太貴，代理中停；dev app 已收，空測試桌留存可刪）。雷：會話環境 ANTHROPIC_BASE_URL 使巢狀 claude CLI 401，啟動 app 或 claude -p 一律 env -u，已記 playbook。已 commit。
- refactor-output-redesign 結案（使用者終判成功）：實測清單全過（含 Dark Wolf 卡驗盤點思考字尾）；追加修復＝鎖定條目可展開唯讀、清單分支不掛指認下拉、思考洩入旁白、重構卡匯新桌誤刪新條目＋undo 復活孤兒、套用介面整份重建清殘渣（拍板 A）。連帶結案 refactor-stream-progress／refactor-review-detail／refactor-outcome-export（DONE 32→36）。新立 character-presence 待辦；refactor-dispatch 補「取消可中止在途＋孤兒子程序」，排下一輪開新對話。
- refactor-dispatch 目標與清單定案（不實作）：按現行程式碼重查現場——finish 階段已不存在（原「finish 下放 fast」作廢）、knownFields 累積是立案後新約束、Cmd-Q 孤兒與無 abort 實錘（cli.rs:707 無 kill_on_drop、lib.rs:2662 無 exit handler）。交接檔新建：三目標（並行省時／檔位省費／取消真停）＋四包清單＋一項待拍板（並行拓撲 A/B）。未 commit（純 .ai/ 變更）。
- refactor-dispatch 拓撲拍板 A＋分工定案（限內部模型）：包 1 主線直寫、包 2/3 外包 sonnet（主線規格＋複驗）、包 4 主線＋使用者。三份 .ai/ 檔同步，未 commit。
- refactor-dispatch 目標模式衝刺：拓撲拍板 A 後包 1–3 全落地——包 1 檔位下放（主線直寫，cargo 438）、包 2 取消可中止在途＋Cmd-Q 孤兒清理（sonnet 代理，cargo 442 含 16-threads 複驗）、包 3 前端 A 拓撲並行上限 4＋首發建快取＋取消接線（sonnet 代理，vitest 94／build／i18n 綠）。三 commit：cce182d／60c58f2／包 3 見 log。雷：巢狀 claude -p 401（OAuth 過期，需使用者 claude login），外包依鐵律例外改會話內 Agent 已報備。剩包 4 實機驗收 P1–P8（交接檔），P7 基線 a/b 待拍板。
- refactor-dispatch 驗收開跑撞 401：根因非本任務——~/.claude/settings.json env 被寫入 cliproxy 代理（7/13 brew 裝、7/29 寫入，來源查無：zsh／Claude 全對話／Codex 皆無蹤，另開對話追）。拍板後備份並移除兩鍵，claude -p 探針 190 秒 401→4.6 秒復通。事故揭三個 app 待修（慢速重試無回饋、安裝鈕沒鎖、探針燒額度）記入交接檔；settings.json env 劫持雷已 append lessons.md。未 commit（純 .ai/ 變更）。
- cliproxy 401 事故收尾：來源查明（7/13 CLIProxyAPI Connect 寫入 settings.json＋~/.zshrc；7/29 mtime＝handoff 技能安裝整檔重寫搬運；斷因＝proxy 內 Claude OAuth 當日 11:21 過期後刷新失敗）；全清恢復訂閱直連（.zshrc 區塊、settings.json 殘留、proxy claude 憑證、殘留終端程序），refactor-dispatch 的 claude login 前置解除。未 commit（機器層設定＋純 .ai/ 變更）。
- refactor-survey-spans 立案（討論拍板：盤點改指位清單——切塊編號＋錨點切點＋涵蓋檢查＋沒點名照舊，app 照位搬字免重抄，輸出降數量級）；於新對話開工，排序在 refactor-dispatch 驗收之後。未 commit（純 .ai/ 變更）。
- refactor-dispatch 實機驗收（orc-cave）＋prompt-cache.jsonl 攤帳：P1 檔位✓（survey=opus-4-7、展開=sonnet-4-6）、P3 快取✓（每筆 cached 11,494）、P2 提速✗（總時長 ~24 分：展開 17 筆 82k tokens、序列鏈段 ~19 分）、P7 拍板 b 且品質✗（純設定整篇重寫、逐日機制「入侵劇情線」未接管）。refactor-survey-spans 升格重構主案（硬指標 <5 分、使用者定形三分類設計：照搬零輸出／接管／需拆丟低階；orc-cave 回歸案例），dispatch 剩 P4–P6/P8 併新案後驗。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 補設計（使用者定形）：判官產出加「分組」維度——跨條目同角色／同機制認組、一條可切段進多組、整組交同一 balanced 代理拆＋併（合併留低階）；與 person-promote 認人機制對齊。NorthHall.png（镇北王府 23 條八角色）實測條目結構後入回歸案例。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 設計強化（使用者指示重想）：四分類加「淘汰」（ST 雜訊零處理、預設移除可復原）、需拆分兩級（乾淨拆＝判官給段→欄位映射 app 零呼叫組裝／糾纏拆＝組代理）、命名權威改判官一次頒布＝序列鏈廢除全並行、結構預掃＋機制守恆稽核（機制標記落照搬堆自動退回重判）、組代理瘦輸入、大組段內指位保險；介面軌時間帳列開工先量。任務檔設計段整段重寫。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 接管路徑細化（使用者確認提問）：蓋章零輸出、轉換走 state-values-mvu 既有 IR 管線輸出骨架、事件敘事本文指位進 payload；美化状态栏改判需拆（欄位定義接管＋顯示殼淘汰）；18k 單筆輸出證非介面（當日未動介面卡），風險註記改為開工對號階段身分。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 淘汰章定義收窄（使用者質疑美化状态栏去向後拍板）：淘汰僅限輸出容器紀律指令，作者設計內容永不淘汰——美化状态栏實看全文後改三路拆（欄位綱要＋在場顯示條件→狀態欄 IR 且 emoji 樣式留資料、行動選項→GM 規則、容器紀律→淘汰）；有 HTML 殼者歸介面線。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 淘汰章改封閉清單制（使用者要求從嚴＋判例入法）：僅①容器紀律②版本標記／更新日誌③ ST 引擎鉤子三類、整條純屬才可蓋且必附編號，混寫→需拆；不在清單或拿不準一律照搬（零成本安全網，作者註記類自動受保護）；稽核缺編號自動退回照搬；淘汰項全文保留一鍵放回。orc-cave 回歸案例補版本標記②。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 以 Transfur.png 盲測（使用者出題）：16 條中 14 條照搬（含目錄型資料與 keyed 地區條），「格式」條實為整套 TRPG 引擎規格＋「COT」敘事指令——抓到真洞＝卡內機制（擲骰／ASCII 地圖）在 app 無機構可接。補三規則：接管判準加反例（靜態目錄／歷史年表＝照搬）、照搬含條目元資料、「未接管機制」後備（照搬進 GM 規則＋成品清單可見）。Transfur 入回歸案例第三張。未 commit（純 .ai/ 變更）。
- refactor-survey-spans 開工（目標模式，代理拍板會話內 Agent sonnet）：主線盤點結案——18k 對號＝機制條目重寫非介面（14 筆＝survey 3705＋重寫 11＋介面 2542＋人物 1；輸出大宗＝思考開銷，照搬滅整筆呼叫為提速主力）、IR 與認人可沿用、apply() 需補元資料保留；小抄合約 v1 定稿入交接檔（六區塊：PERSONS 擴充／INTERFACE／ENTRIES 四分類／SPLITS 七路由／GROUPS／FIELDS）。包 1 落地（sonnet 代理＋主線複驗）：span 切分 byte 相等＋⟦sN⟧ 標記＋預掃＋新 SURVEY_BODY＋六區塊解析器，cargo 442→455 綠，commit feb1cdc。
- refactor-survey-spans 五包收齊（目標模式續，包 2–5 皆 sonnet 代理＋主線逐包複驗）：包 2 本地零呼叫組裝＋四稽核（cargo 463，bc3eef7）、包 3 接管/合組呼叫線＋舊重寫中段刪除（cargo 470，978d46c）、包 4 前端全並行接線（vitest 105，bded948；與包 3 並行發包）、包 5 淘汰/未接管/稽核面板＋i18n 十語系（vitest 108，隨包 5 commit）。使用者拍板：實作對話 context 過大，實機驗收另開新對話——實測清單 T1–T4 已寫入交接檔。
