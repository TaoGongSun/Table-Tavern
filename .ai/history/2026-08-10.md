# 2026-08-10

- ai-card-refactor：實機驗收開跑，展開細看白畫面。修三處——假產物檔停在 person-promote 前的舊契約（單數 `source_uid`）；`parseRefactorOutcome` 補逐欄驗證與 `REFACTOR_IMPORT_INVALID` 訊息（十語系 +1 鍵），壞檔整份拒收不再炸畫面；連帶修 `mergeRefactorInterfaces` 丟掉 AI 產的 HTML 渲染殼（殼從沒傳進 `refactor_apply`，B4 原本必紅）。另依畫面回饋改兩處：結果卡標題「整理好了」→「重構完成」；玩家卡選取從「每個角色都能選＋不指定玩家」改成只問 AI 認定的那一位（勾選、預設勾）。vitest 82（+11）／build／i18n 綠。**未 commit**。
- 假產物路線作廢：手工捏一份逼真的重構產物（先評估西幻卡、後改 orc-cave）等於用人力重造 app 一顆鈕就會產的東西，耗時且測不出真實情況。兩份手工假檔已刪，實測改用 orc-cave-copy.png 真跑。
- 實測順序反轉：先做 refactor-outcome-export（匯出），再從 B 段真 AI 跑起、產物存檔後回頭跑 A 段零額度重放。交接檔 A／B 段照此改寫，B 段測試卡從西幻改成 orc-cave（18 條、6 人、uid 12 同時是介面與機制來源）。
- character-to-player-card：立案（Todo）。角色編輯頁加「升級成玩家卡」入口，補上重構面板只問一次之後改主意的路。
- refactor-outcome-export：立案（Todo），改列為下一個開工項；三項待拍板（uid 對不上、匯出鈕位置、含不含 HTML 殼）開工前先談。
