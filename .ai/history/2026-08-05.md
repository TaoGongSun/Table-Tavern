- card-import-flow：包 3 單一入口＋類型選擇框完成（opus 子代理實作；主線驗收時抓到 V2 獨立世界書 JSON 自帶書名會被誤判成角色卡的缺口，主線補 book_shaped 訊號修掉；cargo 327、vitest 17、build、i18n 全綠）。commit 見下。
- card-import-flow：包 4 第二張卡路由框完成（opus 子代理實作、主線複驗：路由決策純函式＋vitest 5 例、開新桌並匯入 worldId 顯式傳遞、雙世界書封死匯進這桌；cargo 327、vitest 22、build、i18n 全綠）。四包全數完成，等實機驗收。commit 見下。
- ai-card-refactor：與使用者討論後開任務（tasks/ai-card-refactor.md）：AI 重構按鈕視野＝整卡唯讀脈絡、世界書人物條目標記＋在場過濾（不自動轉角色卡、append-only 注入）、西幻世界當驗收樣本卡、按鈕前不燒額度實測（免費匯入看帳本→按鈕→合併實測）。card-import-flow 不做清單改留指標；state-values-mvu 真桌實跑延後合併。未 commit。
- card-import-flow：實機驗收兩件。（1）身分框文案照使用者意見重寫——舊的兩種卡共用文案語氣像在推薦匯入成角色卡（世界書卡照做會玩不動），改成偵測到哪一種就只講那一種，按鈕改「匯入成角色卡／匯入成世界書」，十語系同步、舊兩鍵刪除（build、vitest 22、check:i18n 全綠）。（2）雙世界書封鎖驗收通過（限新桌）；查明舊桌沒有 import-receipts.json 所以擋不住，使用者拍板不補。未 commit。
- card-import-flow：兩句「已只讀入…」腳本提示整組刪除（世界書與角色卡路徑各一句，十語系鍵移除），連帶拔掉沒人讀的死線路（兩個匯入函式與路由的 probe 參數、後端 ImportProbe 的 scripts／alternate_greetings 欄位與偵測程式、兩個只驗這些欄位的測試）。查證結論：條目全部原樣落檔、不執行不等於捨棄，該講的都在機制帳本逐條列著；舊提示只看檔案有沒有 `<%`，連已轉成觸發表的卡也照跳。cargo test 326／build／vitest 22／check:i18n 全綠，淨 −75 行。未 commit。
- card-import-flow：開場白選擇框的「貼出這條」從全文底下搬到框外左下（長開場白不必捲到底才按得到），右下維持「先不要」；使用者實機確認通過。build／vitest 22／check:i18n 全綠。
- card-import-flow：查明訓帝卡沒有卡片介面的原因——它唯一啟用的顯示腳本是雲端載入器（`$('body').load('https://cj15000950009-dotcom.github.io/-/index.html')`），介面本體在作者網站上，我們不抓不執行，所以按鈕不出現、模型的 VN 原文裸露在聊天裡；西幻卡的腳本自帶完整 HTML 才畫得出來。缺口＝那句「介面要連作者網站，不支援」只掛在角色卡路徑，世界書身分匯入時完全沒說明。已補：兩條路徑共用 unsupportedInterfaceNotice()。build／vitest 22／check:i18n 全綠。
- card-import-flow：使用者立規「匯入的處理禁止只做角色卡那條路，世界書要對等」（已寫入專案記憶）。查明成因＝角色卡匯入是最早的功能（7c4f93f），世界書路徑後來才長出來（acf5dcc），新功能都習慣加在舊那條。逐項對過補三處：後端 import_card_extension（世界書路徑也收 extensions.table_tavern 的欄位規則與初始狀態樹）、前端 tellAboutInterface 兩路共用（世界書也講「這張卡自帶介面」）、import_character 改回傳 {meta, book} 讓角色卡也報「已收進 N 條世界書條目」。cargo 326／build／vitest 22／i18n 全綠。
