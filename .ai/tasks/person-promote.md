# Task
Task-ID: person-promote
Title: AI 認人並合併升格：把散在多條的同一角色併成一張角色卡
Status: in_progress
Created: 2026-08-07T23:10:00+08:00
Updated: 2026-08-08T00:50:00+08:00

## Summary
2026-08-07 討論立案。匯入的卡把角色資料散在好幾條世界書條目裡，AI 重構只處理「一條寫了多人」的合集，其餘一律不碰（[refactor_ai.rs:135](../../src-tauri/src/refactor_ai.rs#L135)：「只寫一個人的條目不要列」，理由是 App 已有免費的單人升格）。而那個單人升格是條目編輯畫面裡的「轉成角色卡」按鈕，一次只能轉一條，玩家還得自己從幾十上百條清單裡認出哪些是人。

**實測三張卡，三種壞法：**
- **HeroTrainingUnderSide_stats**（100 條）：15 個人各有一條千餘字元的專屬條目，另有一條「角色概览」合集（5622 字元，一人一段速覽）。照現狀跑，概览拆成 15 張薄卡、專屬條目全留在世界書，13 條 `[Script]XX关系阶段` 機制照搬上桌——關係階段是全的，人卻只有速覽薄卡。
- **NorthHall**（23 條）：8 個人，每人各有「人物设定」與「性格」兩條，外加一條寫了全部 8 人的「角色速览」。照現狀跑，速覽被拆成 8 張簡短卡、專屬條目又各升格一張，**霍玄會有 3 張卡、八個人變出 24 張**。
- **orc-cave-copy**（18 條）：Rigurd 有中文版與 `Rigurd (EN VER.)` 英文版兩條，同一個角色。

**認人近乎免費，合併要花展開呼叫。** 盤點階段 AI 本來就逐條讀過，認人與分組順手完成；資料只有單一來源的人走純本地轉換（[data.rs:1377](../../src-tauri/src/data.rs#L1377)），散在多條的人每人一次展開呼叫（要點 8）。玩家端從「開 15 次條目按 15 次按鈕」變成在既有勾選畫面上一次勾完。

## 設計要點
1. **AI 認人並合併**：判斷這張卡有哪些角色、每個角色的資料散在哪幾條，併成一張卡。
2. **合併必須由 AI 挑段落，不能接字串**：NorthHall 的「角色速览」561 字元裡只有第一行是霍玄，其餘七行是七位長輩——接起來就把長輩的設定全塞進玩家自己的卡。
3. **同一角色的多語言版本挑一版升格**：留跟玩家語言接近的那版，另一版留在世界書裡不刪（細節不丟、角色列表也不會出現兩個 Rigurd）。
4. **玩家人設由 AI 判，不用 `{{user}}` 字串判**：現行單條轉換是看內容有沒有 `{{user}}` 就跳對話問「要轉成你的玩家卡嗎」（[App.tsx:2184](../../src/App.tsx#L2184)），**誤報嚴重**——TestCards 25 張實測 37 條含 `{{user}}`，真正描述玩家的只有 3 條、單張卡最多 1 條，其餘全是「NPC 對玩家的態度」（orc-cave 13 條中招裡 8 條）、回覆格式規則、狀態欄美化、思維鏈規則。改由 AI 在清單裡標出疑似玩家本人的那個人（合併後），玩家勾選時順手確認，整個流程只問這一次；手動「轉成角色卡」按鈕的 `{{user}}` 對話框一併移除，該按鈕一律轉一般卡（玩家卡另有從頭建立的入口）。一桌只能有一張玩家卡（第二張會報錯），單一指定天然不會撞到。
5. **AI 要分得出「人」和「勢力／職階」**：實測卡裡「教会」「贤者议会」「熵增学派」是組織，「圣堂侍从」「奥术学徒」是職階，都不該升格。靠勾選畫面把關，判錯了玩家不勾即可。
6. **勾選預設全勾**。
7. **原條目處置，基準是優先保留而非刪除**：合集條目沿用現行機制即可——被勾的成卡、沒勾的人各自變成獨立世界書條目（標記 `is_person`）、原條目改寫成總述（[refactor.rs:127](../../src-tauri/src/refactor.rs#L127)），沒有人會消失。本任務新增的是合併後的處置：同一人的多條資料併成一張卡後，整條都是他的專屬條目刪除，共用的合集條目移除他那段後若判斷得出只剩分隔線之類的殘渣才刪整條，判斷不出來就留著——留幾條分隔線不影響 AI 說故事，內容消失才是災難。保底做法是整條原樣保留（跟角色卡重複無妨）。刪條目一律走 receipts。
8. **展開一人一次呼叫**：每個合併的人各發一次展開，帶上他名下所有來源條目；一次全包會在大卡截斷，重跑更貴且無解。共用合集條目的改寫等全部人展開完後收尾處理，收尾失敗照要點 7 保底原樣保留。

## Next action
- 實作完成（2026-08-08，cargo 422／vitest 71／build／i18n 全綠），等實機驗收；現場細節見[交接檔](../handoffs/person-promote.md)。
- **本任務先做，[ai-card-refactor](ai-card-refactor.md) 的實測（A–E 共 25 項）等本任務完成後一起跑**（2026-08-07 拍板）：兩者改的是同一條盤點路徑，先測 B／C／E 之後還要再測一次。實作完成後兩個任務一起實測、一起結案。

## Constraints
- 盤點的 system 提示詞必須逐字元相同才吃得到快取，改輸出格式時只動 user 訊息那半。
- 刪條目是破壞性操作，一律走 receipts。
