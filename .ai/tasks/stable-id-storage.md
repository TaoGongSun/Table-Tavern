# Task
Task-ID: stable-id-storage
Title: 儲存改用穩定代碼定址：桌與角色的資料夾／檔名是代碼，顯示名是欄位
Status: todo
Created: 2026-07-27T23:30:00+08:00
Updated: 2026-07-28T00:00:00+08:00

## Summary

2026-07-27 使用者提案：**桌與角色在硬碟上的路徑改用一串代碼，顯示名改成獨立欄位；圖檔與所有引用跟著代碼走。**

現況是「名字即檔名」：`worlds/<桌名>/characters/<角色名>.md`。因此改名＝搬檔＋回填所有以名字為索引的引用，而任何一個沒跟到的地方就會失聯（今天就踩到一個：生成圖庫被寫到 `worlds/` 外面，改桌名後整個圖庫找不到，見 character-card-avatar-issues 交接檔）。

### 這樣改買到什麼

1. **改名不再是多檔案手術**。現在改一個角色名要做 5 個檔案操作（卡片 md／全身圖／頭像／圖庫目錄）＋重寫 3 處引用（所有幕的 speaker、世界書 `visibility.characters`、`state.json` 的 `model_bindings`）。中途失敗（當機、硬碟滿、權限）就停在一半舊名一半新名的狀態。改後＝打開一個檔改一個欄位，沒有中途狀態。
2. **今天這類 bug 從結構上消失**。沒有任何檔案路徑跟著名字跑，就不會有「改名後某某東西失聯」。
3. **命名限制整組拿掉**。同名擋下、`/` `\` 開頭 `.` `..` 控制字元、GM／玩家保留字——這些全部是「名字要當檔名」才存在的。改完之後兩個角色可以都叫「無名者」，桌名可以叫「第一季 / 序章」。

### 不需要中央對照表（主線原本的反對已撤回）

一開始主線反對的理由之一是「代碼→名字的對照表會壞，一壞就全毀」。**這個反對無效**，因為不需要中央對照表：

- 角色卡**現在就已經**把顯示名存在自己檔案裡（frontmatter 的 `name:`）。檔名換成代碼後，`list_characters` 的邏輯一行都不用改——它本來就是打開檔案讀 `name:`，不是看檔名。
- 桌的顯示名塞進每桌自己的 `state.json`（該檔已存在）。列出所有桌＝掃 `worlds/*/state.json` 讀名字。

沒有中央檔案就沒有單點故障。壞掉某一桌的 `state.json`，只有那桌顯示成代碼，其他桌不受影響。

主線原本另外兩點反對（「使用者要能自己開資料夾改檔案」「遷移成本」）由使用者當場否決，且理由成立：產品目標就是讓玩家不必開資料夾；現在只有兩位使用者、只有測試桌、不在意歷史紀錄，是動這種手術最便宜的時機。

## 已拍板（2026-07-28，使用者×fable）

1. **劇情紀錄發言者：每則事件同時存代碼＋當下顯示名快照。** 為何：事件是不可變歷史，角色刪除後舊對話仍有名字；符合「改名後舊對話仍顯示舊名」的既有拍板。
2. **範圍：桌與角色都改。** 為何：只改一半會留兩套定址併存。
3. **代碼格式：ULID，用現成 `ulid` crate。** 為何：天生可排序（時間前綴），不自己發明格式。
4. **舊資料：不寫遷移、不寫偵測。** 新版把 `state.json` 的 id／name 欄位宣告成必填（serde 不給 `Option`），舊資料夾解析失敗自然被略過；兩位使用者更新時人工刪 `文件/TableTavern`。

實作注意：拿掉同名限制後，所有「用名字挑角色」的 UI（發言者選單等）以 id 為值、名字只是顯示；驗收時放兩個同名角色實測。

## 執行安排（已拍板）

- 實作計畫由主線寫（Opus 主線 session 可接），實作發包 general-purpose subagent（`model: opus`），回來驗證：cargo 綠＋npm build 綠＋實跑改名路徑＋兩個同名角色。
- sponsor-features 的圖庫實測、character-card-avatar-issues 的改名提示複驗**併到本案完成後一起驗**（先測會因格式全變而重測一輪）；「生成圖庫目錄放錯層」bug 確定不修，本案完成後自動消失。

## 影響範圍（估）

- 後端 `src-tauri/src/data.rs`：定址方式全面改寫（`world: &str` / `name: &str` → id），幾乎每個 pub fn 都會動；`validate_name` 相關的擋下規則可整組移除。
- `src-tauri/src/lib.rs`：所有 command 參數與 `gallery_dir` 路徑。
- 前端 `src/App.tsx`：所有以名字當索引的地方——`characterImages`／`characterAvatars`（Record 的 key）、`speaker`、`mainView`、`config.preferences.last_world`。
- 估 3–5 小時，期間 app 大概率跑不動。屬「停線」等級改動，要先確認是否插隊到其他待辦之前。

## Next action
- 主線寫實作計畫（含測試清單），過目後發包實作（見「執行安排」）

## Constraints
- **若本提案採用，不要先去修「生成圖庫目錄放錯層」那個 bug**（character-card-avatar-issues 交接檔 Next action 第 1 項）——改用代碼定址後那個問題自動消失，先修等於白工。若本提案否決或延後，才回頭做那個 15 分鐘的修正。
- 改名後舊對話仍顯示舊名，是 2026-07-27 已拍板的行為，新設計不得改變它。
- 側欄卡片高度差（有圖 69px／無圖 44px）是刻意的產品決策，與本任務無關但重構時不要動到。
