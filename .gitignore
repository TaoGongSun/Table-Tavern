.DS_Store
node_modules
dist
src-tauri/target
# codex CLI 生圖時會把圖落在這裡（本機暫存，非產物）
src-tauri/output

# .ai 交接 Markdown 要進版控（! 規則蓋過全域 gitignore 的排除）；本機狀態檔仍排除
!.ai/
!.ai/**
.ai/*.json
.ai/*.jsonl
.ai/**/.DS_Store

# Claude Code 個人設定不共享（launch.json 是本機預覽用的 dev server 設定）
.claude/settings.local.json
.claude/launch.json

# 本機測試用第三方卡片
TestCards/
